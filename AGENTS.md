# AGENTS.md — scenes

## What this is

Browser 3D sandbox scenes that test what a model can build from a single photo. Scene 1 (`index.html`) recreates `japan.webp`, a Kyoto Sannenzaka-style stepped street at sunset with a weeping cherry, in Three.js. The page loads framed exactly like the photo; drag orbits, the wheel zooms, and `R` or the on-screen button resets the photo view.
There is one page for every scene: the dropdown in the corner picks one, `?scene=<id>` names it in the URL, and the registry that lists them is `src/scenes.js`.

Goal: a render that scores as close as possible to the photo on the compare gate while staying a real 3D scene from any angle. Non-goals: photogrammetry, downloaded assets, a build step.

Stack: Three.js 0.185.1 as ES modules through a pinned jsdelivr import map (`three`, `three/addons/`), plain `index.html` plus `src/*.js`, no bundler. Node 24 (`.nvmrc`) runs the gates; Playwright 1.61.1 (chromium) is the only dev dependency. Every asset is procedural.

The current work entry is `docs/work/0_japan-street-scene/plan.md`; it links to the preserved manager-authored phased plan. Scores and their thresholds are in `docs/PLAN-scores.md`. Repo-only rules: `docs/policies/local-rules.md`.

<!-- FLEET-CANON:BEGIN sha=a4df5e4b87dc generated from ../fleet/FLEET.md by `npm run sync-canon` — do not edit inside this block; this repo's own rules go in docs/policies/local-rules.md -->
## Fleet constitution

### Fleet Orchestration Policy

Deliver the requested outcome with verified correctness, coherent architecture, and minimal necessary complexity. Optimize for useful progress—not agent count, code volume, or maximum reasoning. Adapt rigor to risk. This policy grants no additional permissions or capabilities.

#### Roles

Only an explicitly designated agent acts as coordinator. Use one accountable integration owner per scope. The coordinator owns planning, dependencies, shared interfaces, architectural consistency, integration, and acceptance. It does not implement. Every change, however small, is delegated: the coordinator's session is where the next request arrives, and a coordinator in the middle of an edit cannot take it. Its own hands stay on what delegation needs — reading enough to write an assignment, and inspecting a handoff to accept or reject it.

Workers own bounded outcomes and local implementation decisions. Each worker, a reviewer included, decides how many subagents its task needs and dispatches them, within its budget (owner directive, 2026-09-23). For a small task that is none. It remains accountable for what they return. Organize threads around deliverables, not permanent departments. Avoid recursive manager hierarchies.

#### Plan and delegate

Inspect applicable instructions, relevant code/docs, working-tree state, and active tasks before changing anything. Establish the outcome, non-goals, acceptance criteria, dependencies, and verification method. Resolve routine ambiguity through evidence and reversible defaults.

Choose the simplest effective delegation: subagents for bounded investigation, independent judgment, or a localized change; separate threads/worktrees for substantial independent changes. Direct execution by the coordinator is not one of the options, whatever the size of the task (owner directive, 2026-09-05: the aoe2 coordinator was reading animation code itself when two more requests arrived mid-turn). Agree on shared contracts before parallel implementation. Avoid duplicate or blocked work. Size the team to the work continuously (owner directive, 2026-09-15, maps). Widen it when one worker holds the critical path and independent work is waiting. Narrow it when workers contend for the same file, port, or CPU. A worker does not run while another is editing the same file in the same tree, running the same gate, or holding a resource that cannot be shared. An expensive gate is such a resource: exactly one runs at a time, and the other workers are chosen around it (maps: `npm run visual` is a three-hour command that owns a port and the whole CPU).

Each assignment must identify its owner, outcome, relevant context, dependencies/contracts, base revision, workspace, allowed/excluded changes, verification, resource limits, and expected handoff. Specify read-only versus implementation work. Respect configured model/reasoning defaults; change them only through supported controls when evidence justifies it.

#### Coordinate safely

Use only capabilities actually available. Never assume visibility into other chats, shared memory, automatic messaging, workspace isolation, or persistent monitoring. Distinguish prepared assignments from dispatched work and observed status from assumptions. When delegation is unavailable — the session has no way to spawn a worker — work directly or provide an explicit handoff.

Isolate concurrent edits in a worktree: a session or agent doing anything beyond a trivial read works in its own worktree by default, because two sessions in one tree invalidate each other's comparisons, gates, and commits. Serialize overlapping writes only when a worktree is genuinely unavailable, and say why. Finishing a worktree means merging its branch to main and pushing, in the same session — a worktree is never where work is left to sit. Remove it with `git worktree remove` once its branch is merged; a worktree still on disk after its work has landed is a defect to report, not housekeeping to defer, and a session that ends with one says so and where it is. Account for shared services, databases, ports, and compute limits. Never overwrite or discard another participant's work. Track delegated work through completion, cancellation, or handoff, and release only resources you own without losing work.

#### Preserve state and decision boundaries

Keep essential memory in the repository or existing tracker, not conversation history. Reuse conventions and maintain only useful documentation. Record owners, dependencies, revisions, status, blockers, and consequential decisions. The coordinator owns canonical status; workers supply scoped updates. Explicitly propagate changed contracts and revalidate stale information.

Distinguish intended requirements from actual behavior. Preserve evidence, unresolved issues, and next steps at handoff or interruption. Exclude secrets and unnecessary private data.

Workers act autonomously within scope. Escalate cross-task interfaces, persistent formats, security boundaries, major dependencies, and scope changes. The coordinator resolves these within the approved mandate. Unapproved major architecture/product changes and consequential external actions require human authorization. Continue safe independent work while blocked.

#### Implement and verify

Inspect, implement a coherent increment, run relevant checks, diagnose, fix, and recheck. Preserve established architecture; avoid unrelated rewrites, speculative abstractions, and unnecessary dependencies. Proceed beyond planning when implementation is requested.

Use task-appropriate evidence and establish baselines when needed. Inspect actual user flows for interactive products. For games/simulations, check relevant invariants, save compatibility, and realistic-scale performance. Prototype uncertain ideas before generalizing. For research, cite evidence and distinguish hypotheses, measurements, and conclusions.

Obtain independent, preferably read-only review for substantial or high-risk changes when available. Review the exact revision against acceptance criteria. Evaluate findings, fix justified issues, and rerun affected checks. Never weaken tests or conceal failures to claim success.

#### Integrate, report, and stop

Workers hand off outcomes, changes/revisions, checks and results, risks, blockers, and integration requirements. The integration owner inspects actual changes, integrates in dependency order, and verifies the combined result. Worker success alone does not establish integration success. Respect merge, push, deployment, and publication permissions.

After two failures for the same reason, reassess rather than repeat. Unless another budget is specified or the user authorizes continued work, cap automatic repair at five substantive attempts, then report evidence, blockers, and next steps. An explicit user override remains in force for its stated scope; do not ask to renew an overridden default cap.

Keep updates brief and decision-relevant. Clearly distinguish implemented, verified, reviewed, integrated, and blocked work. Stop when acceptance criteria and material findings are resolved; do not invent follow-up work. Report partial completion and unavailable verification honestly.

#### Final acceptance gate

The integration owner is accountable for final acceptance. Before reporting completion:

- Check every acceptance criterion against the final integrated revision, using actual changes and verification evidence—not worker summaries alone.
- Run relevant automated checks and exercise affected end-to-end behavior. Check cross-task interactions, not just each task in isolation.
- For substantial or high-risk work, obtain an independent read-only review of the integrated changes, including integration fixes. If unavailable, explicitly report the missing review rather than implying it occurred.
- Resolve material findings, rerun affected checks, and obtain focused re-review where fixes invalidate earlier review.
- Report the verified revision, checks and results, review status, and any failed, skipped, or unavailable checks. Mark unmet criteria as incomplete.
- Confirm the verified revision is merged to main. Report anything left on a branch or in a worktree, and where it is.

Do not declare the result fully verified while material findings or required checks remain unresolved. Respect the repair budget and report blockers.

### Fleet conventions

- Repository rules add concrete local constraints consistent with the Fleet Orchestration Policy; they do not override its permissions, adaptive workflow, or stopping limits.
- Verify visual work visually: capture the rendered result — screenshot, frame, recording — and look at it, because a passing test says nothing about what the pixels do. Work with no visual surface runs headlessly. For 3D work, one framing is not a check: sweep several camera angles and zoom levels, since a defect the chosen view happens to hide is the normal case. For 2D interfaces and artwork, inspect relevant viewport sizes, scales, and states. For games and other interactive visual deliverables, exercise real controls and representative user flows, and inspect how the rendered result responds over time. For static scenes and assets, inspect applicable viewpoints, using orbit, pan, or zoom in a viewer when available and useful. Use headless interaction and rendering when they provide adequate evidence; use a visible session only when necessary to verify behavior. An aggregate view — a contact sheet, grid, montage, or proof sheet — answers "is there one of each" and never "is each one right", and answers the first just as confidently when the second answer is no: inspect each item at the artifact's own native resolution, and bind the review to the digest of the bytes inspected, so regenerating the artifact strands its review instead of inheriting it. Badge's 63-source contact sheet read as cohesive while ten sources were wrong, and the lesson recurred four days after it was written down, against 48px proof sheets. For visual tasks, inspect the affected user flows and the surrounding rendered result for material regressions; choose the coverage from the change's risk and acceptance criteria. A harness that sets state directly — assigning the camera pose, calling the render function, writing the store — is not exercising the controls, and is structurally blind to every defect living in the input path it skipped, while reporting confidently on the path it kept. scenes applies a camera clamp each frame after the damped controls have run; a diagnostic that assigned camera positions and rendered could not reach the defect the user was reporting, measured the wrong thing, and was used to call it fixed while the user was still watching it happen.
- A defect the user reports is recorded and gated, never only fixed: an entry in `docs/learning/defect-register.md` — symptom as they saw it, investigation, root cause, and how it is checked from now on — plus a check that covers the defect's whole class rather than the one instance. Unlike a lesson, the entry stays after it becomes a gate: the register is the standing list of what the gates could not see, which is where the next defect comes from.
- Gates pass before any commit that touches code; a dependency change re-runs the audit gate. The user has authorized committing, merging to main, and pushing in every repository they own, with no separate approval each time — that standing grant is what the policy's merge and push permissions resolve to. Force-pushing, rewriting history, deploying, and publishing still need the user's authorization.
- For an authorized push, inspect the remote gate through completion when available. Read job steps and runner assignment before treating a failed run as a code failure: exhausted Actions allowance can fail before any job starts. Report unavailable remote verification and use the local gate as evidence within its bounds. Resolve failures that block the requested outcome within the repair budget; report unrelated failures without silently expanding scope.
- The user archives threads, and anything sitting outside main is thrown away with them. So work counts as done only once it is merged to main. A branch, a worktree, or an uncommitted tree does not count, whatever its gates say. Merging is the last step of the work. If something cannot be merged, say so and say where the work is.
- A repo chooses its own language and toolchain — Node, Python, and Rust all run here. Each pins its version where its own tooling reads it (`.nvmrc`, `requires-python`, `rust-toolchain.toml`) and names it in Gates, so a version mismatch is not read as a code failure. Node repos baseline at 24; an older major keeps a CI job proving it.
- Runtime model calls are authorized and already paid for — this fleet has one user, with Claude Code and Codex subscriptions — so a program here may call a model at runtime, vision included.
- When a hard problem blocks progress, use `../fleet/docs/skills/hard-problem.md` to explore distinct approaches. Give each idea a newly spawned independent owner and only the context needed to learn the domain and test assumptions from first principles. Queue waves within available resources; compare evidence before combining routes. Before abandoning a whole strategy, prove it cannot meet the user's requirements under stated assumptions, and obtain independent scrutiny of that proof.
- For substantial or high-risk changes, use independent review when available. High-risk areas include persistence/migrations, security/auth, concurrency, money, supply chain, and edits that reach sibling repos; `../fleet/docs/skills/multi-cli-review.md` provides the review mechanics.
- Error messages are a product surface: check the affected class and relevant adjacent paths. Each names what happened, which input caused it, and what would satisfy it — never a bare `Validation failed`.
- When blocked, hand over the relevant artifact — screenshot, rendered page, log line, data row — with secrets and unnecessary private data removed, as soon as the blocker is named rather than after the analysis: your description of it is filtered through the misunderstanding that caused the block, so it cannot contain what you failed to notice.
- Task-run evidence lives only under ignored paths and is deleted once no active task, unresolved issue, or handoff needs it; it enters Git only when review promotes it into a repository input — a fixture, golden, snapshot, or contract. Tracked docs keep plans, authored review reports, conclusions, and provenance; raw task output stays ignored. Blob ceilings for anything promoted: over 256 KiB needs a stated reason, over 512 KiB binary or 1 MiB of anything never enters ordinary Git, and an asset store or LFS needs the user's approval.
- Keep planning documents and every authored review round in a permanent `docs/work/<id>_<theme>/` folder, with consecutive IDs from 0 independently in each repo. `plan.md` holds status; shared documentation stays in its canonical home. Allocate through the repo's common authority and follow `../fleet/docs/work-docs.md` for formats, review targets, and migration.
- Use the common word where it says the same thing as the rare one. This covers chat, docs, commit subjects and PR titles, comments, and error messages. One idea per sentence, unless another rule asks one line to carry more. Cut length, not facts: keep exact terms, numbers, and the evidence a claim rests on. It applies to sentences you write, not to text you quote or paste. Do not copy this canon's style.
- Write prose one line per paragraph (no hard wrapping).
- Keep a devlog: one short dated line per behaviour-changing session in `docs/devlog/summary.md`, newest first, and a section in `docs/devlog/detailed/` for anything a later session could trip over — what was believed and proved false, what a reviewer caught that the author missed, what number moved and from what. It is history, not status. Both shapes are in `../fleet/docs/devlog-template.md`.
- A lesson is prose only until it is a gate. It lands the session it is learned in `docs/learning/lessons.md` — read at session start — anchored to a measurement, commit, or test id, and naming the gate that will retire it: a test, a lint rule, a schema check, a fixed command. That file is a queue: an entry is deleted in the commit landing its gate, and a gate counts only once it has been made to go red by reintroducing the defect. Deleting the prose is safe only because the deletion is recoverable, so the gate carries the claim in its own header and `docs/learning/gate-proofs.md` carries the mutation, the failure it produced, and the pre-retirement commit that `git show <sha>:docs/learning/lessons-evidence.md` reads the whole evidence file back out of. A gate and the claim in its header can be wrong together and look exactly like a gate that is right, and auditing one means reaching what was believed, measured and abandoned at the time — never the sentence the gate carries about itself. An entry that can name no gate is not a lesson — fleet-wide knowledge is staged in `canon-candidates.md` for this constitution, repo-only knowledge goes to `docs/policies/local-rules.md`, and the rest is folklore and is dropped. An index that only grows is a list of things that failed to graduate: aoe2's reached 84 entries, none naming a gate. An index already holding ungated entries is emptied entry by entry as each one's area is next touched, not kept as a standing exception. Shape: `../fleet/docs/lessons-template.md`.
- A green gate proves less than it looks like. Every gate is bounded by something — a seed set, a tick window, a resolution, a fixture that ends early, an include list, a shared flag any one case can satisfy — and proves nothing past that bound, so name the bound in the gate's own header and pin every input that reproduces the defect, not just the one you thought of. Past its bound a gate does not merely miss the defect — it can measure a different phenomenon entirely and report it just as confidently: aoe2's stone-mining window opened at tick 11,000 on a fixture that resolves by conquest at 11,442, so it was reading a decided match, and a finished match and a deadlocked one are pixel-identical. A gate that cannot tell "passed" from "did not run" reports the second as the first, and a check built from the same symbol as the thing it checks proves only that the code agrees with itself. Retiring 356 lessons across 14 repos found **more than 40 whose evidence named a live, passing test that did not catch the defect** — every repo's `docs/learning/gate-proofs.md` records its own: an 800-decision rollout window hid a divergence that starts at 3,000, a Rust-side struct pin stayed green while the WGSL side it mirrors gained a field, and `replaceFootprintOwner(…, undefined)` passed all 873 tests.
- A command's exit status is a claim about the command, not about the work: a pipeline exits with its last stage's status, so `npm run x | tail` reports tail's success over any failure; `git add` fails all-or-nothing while the `commit` after it still succeeds and ships a message that lies about its contents; and a tool reporting that it applied a fix reports a no-op identically to a refusal — read the artifact it should have changed. Red deserves the same suspicion as green: a non-zero exit is equally a claim about the command, and a missing dev dependency, an unresolvable binary or a wrong working directory fails identically to a broken product — aoe2's `playtest:corpus` went red on a merge because `tsx` was gone from `node_modules`, not because the code was wrong. A blocker you inherited is the same kind of claim: retest it before repeating it, because its whole effect is to stop work. A RETURN VALUE is a claim of the same kind: a search that cannot match hands back a sentinel the next call uses without complaint, and an `indexOf` miss fed to `slice` deleted 319 lines of a tracked spec in one edit.
- Verify the instrument before trusting the measurement, because a critic is a backstop and not the first line. Confirm the flag took effect, the denominator is the population you meant, the control reproduces, and the claim you are relying on is still true rather than remembered. A whole session's conclusions were built on labels chosen with knowledge of the future, agreement quoted over a population that was 99.8% forced no-ops, a `--eval-episodes` flag silently ignored so every checkpoint was picked by a five-sample lottery, and a review lane declared unavailable from a three-week-old memory that was wrong. Each was one command away from being caught. An A/B comparison asks one more thing: that the tree HOLD STILL. Edits landing between the two arms make them differ by more than the variable under test, and the result reads as a finding — aoe2 read a boot-map regression off two arms it had edited between. A repo that ships a debugging instrument — a session replayer, a recorder and bundle differ, a capture script, a profiler, a debug probe — is asked FIRST, and the task's first probe names which of them answers the question or why none does. aoe2 skipped its engine's replay harness twice with the rule in prose (2026-06-13, 2026-09-05), the second time with a memory stating the rule loaded in the same session; what held was a hook that refuses to write or run a scratch probe without a `harness:` line naming the tool. A rule that must be remembered at the moment of writing a probe is enforced at that moment, not read at session start.
- Anything slow on the critical path is a defect to identify and fix, not a cost to schedule around (owner directive, 2026-09-15). Keep only the slowness a verdict depends on. What remains is the expensive gate the team is sized around (scenes: every gate ran through a CPU rasterizer on a machine with a GPU, a fourteen-second frame on every round-trip).

- A standing loop sources its next task by running the artifact the way its user does — the default entry point, the real configuration — never by reading code for something to improve. A repo with no runnable entry point has no standing loop.
- This harness compacts context mid-task: a compaction is a harness event, not a task boundary, and never a reason to wrap up or hand off to a fresh session.

- Before resuming interrupted work, inspect surviving worktrees, artifacts, and task status. Recover what exists and preserve unfinished work; do not assume that a worktree, transcript, or shared memory survived.

- Steering compounds: a direction that outlives the immediate task lands that same session — `../fleet/FLEET.md` if fleet-wide, else this repo's `docs/policies/local-rules.md` — and you say where it went.
- Reviewer model pins live only in `../fleet/docs/skills/multi-cli-review.md`; a model a product itself calls is pinned in the repo that calls it. Never hardcode a model ID anywhere else.
<!-- FLEET-CANON:END -->

## Gates

- `npm test` runs `import-inert`, `shot`, `compare`, `placement`, `namerules`, `clearance`, `animation`, `nudge`, `blackframe` and `record`, then asserts the scores against the thresholds in `docs/PLAN-scores.md`. Needs network for the Three.js CDN and a cached Playwright chromium (`npx playwright install chromium` once).
- `npm test` also requires each tool to prove it ran: it captures the tool's output and fails, naming the tool, if the summary line that tool prints at the end of its checks is missing. A tool that exits 0 having done nothing used to be reported here as a pass, which is what `blackframe` and `record` did on every CI run until 2026-09-09. It prints each gate's elapsed seconds too, so a log carries what each one cost. Those markers are plain substrings, so a tool that renames its summary line goes red until `GATES` in `tools/test.js` is updated.
- **Which renderer a gate uses is a decision each gate states, and every gate prints the renderer it actually got.** A gate renders on the GPU unless its verdict depends on the CPU rasterizer; `shot` and `compare` are the only two where it does, and they have no switch at all. `<NAME>_GPU=0` forces one gate onto SwiftShader, `<NAME>_GPU=1` forces one onto the GPU, and `GATES_GPU=0` moves all of them — the gate's own variable wins, which is what lets CI say "software everywhere except blackframe". Asking for a GPU is a request: chromium falls back to SwiftShader where there is none, so the printed tag says `[GPU]`, `[software]`, `[SOFTWARE FALLBACK: the GPU was asked for and none was available]`, `[GPU, though software was asked for]` — which is what `shot` would print, and it now fails on it rather than writing a GPU frame to the contract — or `[UNRECOGNISED: …]` for a renderer string that names no device. **One gate carries per-renderer limits**, `nudge`, because its whole reading is shimmer and a GPU and a CPU rasterizer do not shimmer alike; the limits are keyed on what the run got, never on what it asked for. Everything else reads the same on both, measured (2026-09-15) rather than assumed — including `animation`'s scored thresholds, which briefly carried a renderer margin until a review measured that the margin was pure slack on the renderer the gate now runs on. The whole exercise and its numbers: `docs/devlog/detailed/2026-09-15-gpu-gates.md`. **How far apart the two renderers score one frame is not a constant of this repo, and it was being read as one.** Measured on four trees on 2026-09-17: SSIM's crossing is stable, the GPU reading 0.00080 to 0.00091 better everywhere, but cell distance's swung from 0.000103 better on the 2026-09-15 tree to 0.0000036 better on iteration 5's and 0.000069 WORSE on a tree with the ambient light moved — it changes sign with the scene. Nothing anywhere may size a limit from the single figure the 2026-09-15 line quotes; `npm run try` carries the measurement and the method, and `docs/devlog/detailed/2026-09-17-fast-arm.md` has the table.
- CI has no GPU. It sets `GATES_GPU=0` and `BLACKFRAME_GPU=1`, which gives every gate there exactly the chromium flags and exactly the limits it had before any of this, and makes the renderer a stated choice instead of a property of the runner. `GATES_GPU=0 BLACKFRAME_GPU=1 ANIMATION_FRAMES=3 BLACKFRAME_VIEWS=1 RECORD=0 npm test` is that run, locally.
- `npm run import-inert` imports every tool in `tools/` in its own child process and fails unless each one loads and does NOTHING: exits 0, prints nothing, finishes fast. It exists because importing `tools/animation.js` started a full animation run on main until 2026-09-15, and importing `tools/test.js` ran the whole suite — seven tools had no main-module guard at all. A tool's main block goes inside `if (isMainModule(import.meta.url)) { … }`, with `isMainModule` **imported from `tools/serve.js`**, never hand-built. It opens no browser, costs about 7 s, and runs first in `npm test`. Its other half is the evidence-marker check: this one catches a tool that runs when it should not, that one catches a tool that does not run when it should.
- The suite costs about 3.6 minutes on this machine, sequential: import-inert 4 s, shot 63 s (SwiftShader, the contract), compare 1 s, placement 11 s, namerules 9 s, clearance 32 s, animation 12 s, nudge 15 s, blackframe 43 s, record 28 s — **218 s in all**, measured 2026-09-16. An earlier run the same day over the same gates read 191 s, and one before `namerules` existed read about 168 s: the per-gate numbers move several seconds run to run with machine load, `shot` by twelve between those two runs, so treat any one of them as a reading and not a budget. It was 35 minutes until 2026-09-11 and about 10 until 2026-09-15; `shot` is now 40% of it and is the one gate that cannot move. Where the earlier difference went is in `docs/devlog/detailed/2026-09-11-suite-cost.md`, along with the measurement that says running the gates concurrently would take another 30% off and why it was not taken — a saving worth much less now that only `shot` and `record` are long.
- Every gate prints `scene ready in <n> s` per page it opens, against a 300 s ceiling. The ceiling is what a slow shared runner needs and costs a fast machine nothing; a page that fails to load a script or throws still fails at once. The wait awaits `index.html`'s own `window.__sceneReady` in one evaluate rather than polling `window.__sceneState`, so the number is when the scene became ready and not when a poll next got a turn.
- **A `page.evaluate` on the scene page waits for the frame in flight**, and that frame is the whole post chain. **Two numbers, and they are not the same number**: the FRAME at 1200x1100 on this machine's SwiftShader is about **5.0 s** (composer target 1440x1320 at 4 samples — the shipped 8, clamped), and a ROUND-TRIP to that page costs about **14 s**, because the evaluate waits out the frame in flight and then its own, plus CDP. The 5.0 s is corroborated twice outside the probe that measured it: `animation`'s own per-frame line reads `pose and two frames 15.4 s`, which is `setTime`'s render plus two loop frames, and the whole `shot` path (one evaluate of two rAF, then a compositor screenshot) is 36.0 s. On a shared runner both are several times larger; on this machine's GPU the frame is about 5 ms. **What the 5.0 s is spent on is not known** — two attempts to break it down both died on the same three.js fault, `Invalid value used as weak map key` in `WebGLState.drawBuffers`, reached by touching `renderTarget.samples` or `composer.setSize`; the second attempt's control row then read 0.02 s against its own 5.01 s baseline and the whole table was void. A third attempt should toggle `Pass.enabled` only and re-assert the baseline after every toggle. So the rule bites on the software rasterizer and is nearly free on a GPU — which is why moving the gates to the GPU on 2026-09-15 was worth more than every round-trip saving before it, and why the rule still matters: CI is SwiftShader and always will be. A round-trip to that page is never free there, so a gate that is slow is asked how many evaluates it makes before anything else. Image work never belongs there at all: `openInspector` in `tools/lib/browser.js` opens the blank page that every decode, resample and compose runs on, and it collects that page's errors into the caller's own array so moving work off the scene page does not move it out from under `collectErrors`. The same `decodeImage` call on the same file in the same process cost **96.1 s on the scene page and 0.1 s on the blank one**, and returns the same 1,320,000 bytes either way — byte-identical, with the scores off them equal as doubles (`out/scratch/decode-purity.mjs`). `tools/animation.js` was doing eight of those on the scene page.
- `npm run placement` runs two checks the scores cannot make. `PLACEMENT_CHECKS` casts the photo camera's ray through each listed position and fails unless the first mesh hit is the named one (a piece built inside another, a canopy hiding a missing trunk). `GROUNDING_CHECKS` takes each listed object's own base, drops a ray, and fails when the mesh it should stand on is too far below it (floating) or too far above it (buried). Both lists are in `src/layout.js`. Both halves are cast in one `page.evaluate`, with the clock pinned first inside it; `clearance` pins its clock inside its own measuring call for the same reason. **Renderer: the GPU**, and it waits for no settling frames, because no pixel enters the verdict — both halves are `THREE.Raycaster` against the scene graph on the CPU. Measured on 2026-09-15: the same 21 forward hits and 10 grounding results on both renderers, 7 s against 26 s standing alone (8 s in `npm test`, against 30 s before). Iteration 3 then grew `PLACEMENT_CHECKS` to 23, and re-measured on 2026-09-16 both renderers print 23 forward hits and 10 grounding results, 10 s against 24 s.
- `npm run namerules` pins the population of every rule in this repo that selects meshes BY NAME, against `tools/name-manifest.json`, and fails when any of them moves. A mesh name is an interface between rules that were written apart and do not import each other: `PLACEMENT_CHECKS` in `src/layout.js` matches by name PREFIX, and `NO_CAST` in `src/lighting.js` is a name regex deciding which meshes cast shadows. Iteration 4 renamed `far roof ridges` to `far house ridges` to stop it widening the first, which moved it across the second and put 37 instances into the shadow pass; the shot went 364 to 366 draw calls and **every gate stayed green**, because `placement` only asks that the check still passes, `shot` prints the draw count and asserts nothing, and `perf`'s budget is 400. The only signal was a two-call move in a printed number, and it was explained away in writing before a second critic round decomposed the frame. **53 rules** are pinned today: `NO_CAST`; `clearance`'s own `WALK`, `TERRACE`, `TERRAIN`, `LIVING`, `CANOPY`, `SURFACE_WORDS`, `PLANT_WORDS` and `BEYOND_BLOCKERS`; clearance's inline `o.name === 'cherry trunk'` lookup; and one population per `PLACEMENT_CHECKS` entry (23) and two per `GROUNDING_CHECKS` entry (20, its object name and its ground prefix). **The rules are read out of their own files and evaluated, never re-implemented** — `NO_CAST` is a `const` and clearance's lists are locals inside a `page.evaluate` argument, so neither can be imported; the tool takes the text of the single-line `const NAME = …;` declaration and builds it with `new Function`, records that text in the manifest so an edit that moves no population is still red, refuses a `g`/`y` regex whose `lastIndex` would make the answer depend on the order names were tested, and **counts every `const <ident>` binding in the file first, in any shape, refusing unless there is exactly one** — a critic showed that counting only single-line declarations let a rule reformatted across lines go unnoticed while a stale one-liner left in a comment or a template literal was read and reported instead, green. The populations come from the BUILT scene, in one `page.evaluate` that returns every mesh's name, `castShadow` flag and instance count. **The two halves cross-check each other where the defect lived**: every mesh `NO_CAST` matches must have `castShadow === false` in the built scene, so inlining a different regex at the application site is red (23 meshes named). It also asserts `shadowCasters`, the count `applyShadowFlags` returns — that is the term the rename moved, with no camera in it, and it is cross-checked against the meshes whose `castShadow` this run can actually see, because those are two different traversals. And it scans `src/*.js`, `tools/clearance.js` and `tools/placement.js` by line text for the five usual matchers plus any line that mentions a name and compares, splits or looks it up (**17 sites today, 14 of them mesh rules**; the other three match objects and a CSS colour), so a NEW rule nobody registered is red. That scan matched five method names until a critic found the rule it could not see — clearance's own `o.name === 'cherry trunk'`, in a file it already read — which made the gate's claim to cover every mesh-name rule false; taking every string method instead reached 41 sites, 20 of them `.replace()` on GLSL, so the widening is targeted at names rather than at strings. Updating is `node tools/namerules.js --update "<why>"`; the reason is required and must differ from the recorded one. Bounds, and they are real: **one command rewrites the manifest**, so this makes a rename visible, not impossible — and that one command clears every class of finding at once, including a rule nobody registered, under a reason that may mention only the rename; it pins populations and not correctness, since a right rename and a wrong one move a population identically; the site scan does not cover this file, other tools or `tools/lib/`, and is still blind to a rule that holds the name in a variable spelled something else while using none of the five matchers; **every clearance rule is applied here independently while clearance classifies with a first-match-wins chain**, so `LIVING` is pinned at 23 where that gate builds 21, and reordering the chain moves clearance's real sets with this gate green; and draw calls and instance counts are printed and NOT asserted, because the first moves with frustum culling and canvas size and the second with the canopy's PRNG. Not proved red: the five floors, calibrated from the shipped figures and nothing else. **Renderer: the GPU**, no settling frames, no pixel in the verdict; `NAMERULES_GPU=0` (or `GATES_GPU=0`) forces SwiftShader. Measured rather than assumed: SwiftShader prints the same 53 rules, 255 meshes, 98 shadow casters and even the same 370 draw calls and 745,206 triangles, so the manifest is a property of the scene and not the driver and CI is held to the numbers recorded here. About 9 s in the suite.
- `npm run clearance` asks what the walkable street keeps clear, overhead and at ground level, in two checks off one page load. Both measure the built scene rather than the constants it was built from — it imports nothing from `src/` — because both defects it exists for were introduced by editing exactly the constants a source-derived check would have read. **Headroom**: every blossom instance matrix, every strand vertex and every trunk vertex, against the paving found by a ray dropped under it. Nothing the cherry hangs may come below 1.90 m over the street or over the right terrace (a standing eye is 1.70 m, plus 0.20 m), and its low wood must all sit within 2.00 m of its own deepest point rather than being a limb at head height. The photo's blossom mask is a 2D silhouette and cannot bound depth, and the canopy once reached 1.71 m over the landing with every gate green. **Street**: the paving's own bounding box is swept, the paved band's edges and centre found at each z, and both halves must keep 0.60 m of run clear of anything built between 0.35 m and 2.0 m over the paving, with the near half keeping 0.55 m clear of eaves as well. Each check also asserts it measured something, because a run that found a fragment prints the same word as a run that found the street: 40,000 canopy samples, 1,000 of them over paving, a trunk mesh and terrace paving existing at all, 50 structure meshes, a structure reaching the paving at a fifth of the street's z positions, and walkable paving under 90% of the sampled z between the swept window's top and z = -33.5. **And it checks its own classification.** The building set is built by exclusion — anything the walkable, terrace, terrain, plant and canopy lists do not match is a building — and all of those are whole-name anchored regexes, so a new mesh is never near a match and a new PAVING or a new PLANT lands in the building set in silence. Iteration 3's `right walkway slabs` and `fence spill leaves` both did, for a whole iteration, with every assertion green; the terrace headroom rays were landing on the band under the flagstones, up to 1.02 m below the surface at the terrace's steep end. So every set is printed by name, the terrace among them since 2026-09-16, and a mesh in the building set whose own name contains a paving word or a plant word fails the gate. Its bound is real and is measured against the scene's own names rather than invented ones: it is a word list, printed every run and recorded in `out/clearance.json`, and over the sets that are correctly classified today it would catch only **WALK 6 of 14, CANOPY 1 of 2 and LIVING 7 of 33** — blind to `stairs`, `gutter`, `landing`, `far street`, `platform`, `cherry strands`, `cherry trunk`, `pot` and more, so `landing 2` or a fourth pot would still land in the building set in silence. It also looks one way only: a building misfiled as terrain or a plant is caught by nothing. `plant` is deliberately NOT one of the words, because `planter` is this repo's own name for the terrace's stone bed in eight places and that bed is masonry — the failure text would have told someone to move a real obstruction into the plant set. Add a word when a new name needs it, never to close the gaps above: a word added for a mesh nobody has written cannot be red-proved. One plant is a known, measured hole rather than an oversight — `fence spill leaves` hangs 1.44 m over the terrace by construction, so it is classed as a plant and printed, not asserted. Bounds are in the tool's header; the ones to know are that it excludes the petals (they are built to fall through the street, and a petal's drawn height is a shader function of the clock rather than its instance matrix), that it reads CPU matrices and so would miss a wind with a y term, that it asserts a clear RUN in each half rather than a clear half, that it measures the far row's wall line for the 0.60 m rule and its eave line for the 0.55 m one, and that the street stops at z = -33.5 — past there the corner house's roof oversails the last of the paving, which is the scene's own open defect, printed rather than asserted, with the exempt region pinned to no more than **6 rows and to `corner house roof` alone** so a NEW building there is red. That pin was 40 rows and three meshes until 2026-09-16, written when `far street slabs` ran to z -42 and 7.5 m of paved road lay under three buildings: 34 rows, 27 of them blocked. Iteration 4 ended the paving at the corner house's own front face and left 3 rows, 1 blocked, by one mesh — so the old pin was five times the region it guarded and a whole new building standing across the road inside those 40 rows would have passed the row count. 6 is 3 plus a stated margin of 3 rows (0.75 m), which is what the sweep's own 0.25 m grid and a small move of `FAR_STREET_END` can shift without anything being built. Both halves are red-proved: relaying the paving to z -42 fails on the row count **and** on `corner house body` and `bend roof` no longer being exempt, and that same mutation passes with exit 0 under the old 40-row, three-name pin. It writes `out/clearance.json`. **Renderer: the GPU**, and no settling frames either, for `placement`'s reason — it reads instance matrices, vertex positions and raycast hits, never a frame. Measured: every printed figure identical on both renderers, to three decimals and the same coordinates, 14 s against 24 s standing alone (12 s in `npm test`, against 21 s before).
- `npm run groundcover` asks whether the ground covers the world, in the one scene that has a world to cover: it opens the `whitehouse` scene URL, pins the clock, and drops one `THREE.Raycaster` ray straight down from y = 400 m on a 20 m grid over x +-640 m and z +-900 m (5,915 cells), failing if ANY cell's first non-sky hit is nothing, with a 5,000-cell floor and a floor of exactly one mesh named `sky` so a sweep that collapsed cannot read as a pass, and it imports no `src/whitehouse/**` module because the defect it exists for was a slab that was never where the source said. **No scored metric can see a ground void**: `compare` reads sky through a hole as just another colour, and the pass that closed the first one — a 40.6 m by 1040 m trench the sky showed through — moved both scores the right way for entirely unrelated reasons, so this gate counts rays rather than pixels and was red-proved on the real defect before the fix (**4,166 of 5,915 cells blank**, green at 0 of 5,915 after). It is scene-scoped to `whitehouse` in `tools/test.js`'s `GATES` and is **skipped for every other scene with its reason printed** (the slabs it sweeps and the camera clamp its extent comes from are this scene's own, and scene 1's 30 m street is not a ground-cover question), the same generic skip path scene 1's own gates take in a `whitehouse` run; its bounds — a hole outside that box, narrower than one 20 m cell, or hidden under another mesh is not seen — are named in its own header.
- `npm run animation` scores the photo view at seven points across the animation cycle and fails if the worst of them leaves tolerance. The shot gate pins the clock at t = 0, so it alone cannot see a drift that only shows once the canopy has swung. `ANIMATION_FRAMES` trims the list; the motion reading moves with the spacing and the `0.4` ceiling does not, measured at 0.15 levels over seven frames, 0.19 over three and 0.24 over two on the GPU (the default local run) and 0.14 / 0.18 / 0.22 on SwiftShader (CI), so a trimmed run sits nearer that ceiling and its number is not comparable to a full run's. Each frame prints what it cost, split into the pose, the screenshot and the decode. Rendering these frames smaller is not available: the gate holds the worst of them to `docs/PLAN-scores.md` plus 0.0006, which is the shipped frame's own threshold and belongs to the 1200x1100 contract — on SwiftShader the t = 0 frame is byte-identical to `out/render.png`. **Renderer: the GPU, 11 s against 277 s** (`npm test`'s own lines), and the scored thresholds are the same on both renderers: on this machine's GPU the same frame scores about 0.0009 better on SSIM and within 0.0001 of the software frame on cell distance, so it sits inside the unwidened thresholds with at least as much room as a software run has. That sentence read "0.0001 better on cell distance and 0.0009 better on SSIM" until 2026-09-17, when the crossing was measured on four trees instead of one: SSIM's half held (0.00080 to 0.00091) and the cell half did not — it was 0.000103 better on the tree it was first measured on, 0.0000036 better on iteration 5's, and 0.000069 WORSE with the ambient light moved. It is a fact about a tree, not a constant, and it does not always favour the GPU. The cell headroom at iteration 5's score is 0.0015 (0.0611 against 0.05958), **14.8 times** the largest crossing in that set, and this gate adds its own 0.0006 allowance on top, which makes 20.6; either way its verdict is unaffected. (An earlier draft of this sentence said "more than twenty times" against the 0.0015 figure, which is false — 20.6 belongs to the other denominator, and a critic caught the two being mixed.) What the old wording would have done is let someone size a TIGHTER limit from a number that moves. See `npm run try` and `docs/devlog/detailed/2026-09-17-fast-arm.md`. A renderer margin was briefly added here and removed after a review measured it as pure slack; the note above `OUT_DIR` in the tool says why, and the point generalises — a widened limit is a limit nobody can red-prove. The worst frame is also held to this run's own t = 0 frame, which is a restatement of the spread bound anchored somewhere meaningful rather than a second bound. The spread and motion bounds never cared which renderer drew the frames, only that one renderer drew all of them.
- `npm run nudge` renders three poses twice each (identical output required), then moves the camera 2 mm and fails when too many pixels change drastically. It exists because a user saw the scene flicker while orbiting and no scored gate could see it: a still camera renders the same frame every time. The scene is rendered above canvas resolution (`POST.renderScale`) for the same reason. **Renderer: the GPU, 10 s against 170 s, with its own limits** — this is the gate where the renderer was never neutral. The user had a GPU, and a GPU gets the eight multisamples SwiftShader clamps to four, so a SwiftShader run was comparing the supersample alone with the multisampling held at the broken value in both arms. On the GPU the same mutation separates ratio 1 by 5.1x where SwiftShader manages 2.2x, and the old limits would have let the broken scene pass ratio 1 outright. `LIMITS` in the tool is keyed on the renderer the run actually got, so a fallback to software is held to the software numbers and CI's verdict is unchanged.
- `npm run blackframe` loads the scene at six window size and device pixel ratio combinations (including non-round ratios), measures the frame on load and again after a resize, and fails when the post chain hands back a black, flat, or nearly lightless frame. It is the counterpart to `nudge`, which scores how much the frame *changes* and therefore rates a fully black frame as perfectly stable; this one scores whether the frame is *there*. **Renderer: the GPU, and it is the one gate that asks even where the others are told not to** (`BLACKFRAME_GPU=1` beats `GATES_GPU=0`, which is how CI is written) — the defect it guards is a driver behaviour, so it asks wherever it runs, prints the renderer it got, and falls back to SwiftShader where there is no GPU. `BLACKFRAME_VIEWS=<n>` trims the sweep and the run prints which views it covered; CI runs one, the size the user's report came from. The two frames a resized view settles for are waited for inside the measuring call rather than in a `page.evaluate` of their own, which is the same frames and one round-trip fewer.
- `npm run webp` encodes `out/render.png` as `docs/render.webp` (under 256 KiB) through chromium's own encoder.
- `npm run shot` renders the photo view at 1200x1100 in headless chromium to `out/render.png` and fails on any console error, page error, or failed request. That frame is the contract `docs/PLAN-scores.md` records, so nothing in the tool may change the pixels: hiding the page's chrome, pinning the clock and waiting two frames for the compositor are one evaluate rather than three, and the file is byte-identical either way (sha256 `d29b76dd0272…` on SwiftShader). **Renderer: SwiftShader, and this is the one gate with no GPU switch at all** — the scored number has to be the same digits here and on a runner, and a CPU rasterizer is deterministic across machines while a driver is not. A flag that could move these pixels is a flag that could move them by accident, and the tool now **fails** rather than shooting if chromium hands it a GPU anyway — a GPU frame would score 0.0748 / 0.4742, inside the thresholds, and rewrite the contract on a green run. **Since 2026-09-17 it also records and refuses the configuration the frame was drawn at.** The sidecar carries `post` — the rung, its cost, the scale, the target size, the sample count, the numbers `verifyComposer` measured (badTaps, meanLuma, referenceLuma, lightKept), the watchdog's counters (floor, ratchets, contradicted readings, and whether it has switched itself off), and how long the scene took to become ready — read out of `describe().post` as the last act of the evaluate the tool already makes, so it costs no round-trip, and printed. It **fails** on four things the contract frame must never be: a rung `src/post.js` does not mark as the chain AS DESIGNED, a watchdog ratchet, any `post:` warning the page printed, and a configuration that changed between readiness and the shot. **The predicate is `designed`, not `rung === 0`**, and that is measured rather than stylistic: the rung is an index into the rungs that fit the memory budget at this size, so on a large high-density window index 0 of what survives is already a lesser chain, and a gate that asked for index 0 would have written it to the contract (red-proved with `TARGET_BYTE_BUDGET = 1`). Those are four ways of asking one question because each is blind somewhere — the rung is a number the page reports about itself, the warning is a string `src/post.js` could stop printing, and the pair only differ if something re-tuned inside the tool's own window. The five rungs available at 1200x1100 on SwiftShader produce five different files (`e95a53185ee3`, `1f74009907c0`, `1825c9dd2e0f`, `d1d5527ad529`, `e11e4d5135b8`), so a step-down really does rewrite the contract. **And it has already caught one**: 1 of 25 fresh runs of this tool on this machine ratcheted its watchdog on a healthy frame and drew `1f74009907c0`; that cause is fixed in `src/post.js` and this refusal is what makes the next one of its kind red instead of scored. **Its bound is stated in the tool's header and is large: it did not catch the flake it was written for.** The 2026-09-17 frame that scored 0.0632 instead of 0.05958 from the same tree was drawn at the designed rung with the right size and samples; what separates it is the recorded verdict (referenceLuma 54.6 and meanLuma 72.7 against 57.5 and 76.8). No ABSOLUTE figure for that is asserted anywhere, because the light in this scene is a property of the scene and an iteration lane moves it every iteration; what asks the question is `compare`'s same-tree light check, below. The refusal covers the configuration; the record covers the diagnosis. `docs/devlog/detailed/2026-09-17-shot-rung.md` has the whole measurement. **And since later the same day it refuses the LIGHT as well**, which is the half aimed at that flake rather than at its neighbour: the sidecar carries `env` out of `describe().env` — the map's own radiance, its shape, the per-material binding and what it adds to the photo view — and this gate fails when `src/lighting.js` could not prove the map, or when it had to build the map more than once. That second one refuses a frame that may be byte-identical to the contract, deliberately: a first build that carries no light is the 2026-09-17 event happening on this machine, and a green run would bury it. Both cost no round-trip; they ride the evaluate this tool already makes. Proved not to move a pixel — `out/render.png` is byte-identical across the change, sha256 `ef7a32617aa6f91f` on SwiftShader. It is 64 s of the 161 s suite and it stays there.
- `npm run compare` scores `out/render.png` against `japan.webp`: mean color distance over a 24x22 grid of cells (lower is better) and grayscale SSIM at 64 px (higher is better). It refuses a render older than the source. **Renderer: SwiftShader, no switch either, for a different reason than `shot`'s** — it renders no 3D at all, but it decodes and resamples both images through chromium's 2D canvas, and an accelerated canvas would make the scored digits a property of the graphics driver. On this machine the decode came back byte-identical either way; the lever is absent because the measurement covers one machine and the contract covers every machine. **It refuses nine things, not one**: a render older than any source file (by mtime, which is the 2026-09-05 rule), a missing `out/render.tree.json`, a sidecar whose recorded render digest is not the `out/render.png` on disk, a recorded scene source tree that is not the tree on disk, a sidecar with no post-chain state or no verdict inside it, one whose post-chain rung `src/post.js` does not mark as the chain as designed, a sidecar with no environment state or one of a shape it does not know, and one whose environment map `src/lighting.js` could not prove or had to build twice — the second, third and fourth are the score's half of the binding that replaced `1-photo.png` being byte-identical to `out/render.png`, and they catch what an mtime cannot: a source reverted to an older copy, a checkout that rewrote mtimes, a render carried in from another tree. The fifth and sixth are the 2026-09-17 POST half: the thresholds in `docs/PLAN-scores.md` are the FULL chain's numbers, and a frame drawn at a lesser rung scores differently while looking, in every other field of the sidecar, exactly like the contract. Their bound is that they read a number the page reported about itself, and that they are blind to the flake they were written for, which was drawn at rung 0. The seventh and eighth are that day's LIGHT half and are not blind to it: the thresholds are also the fully lit scene's, and the frame this scene draws without its environment map scored 0.0632 against 0.05958, which fails them. **And a ninth check is aimed at that flake's own class from the other side: the same tree must draw the same amount of light.** When the previous `out/light-anchor.json` is of the same scene source tree, its `referenceLuma` — `verifyComposer`'s own 64x64 render of the scene with no post chain, at a clock pinned to 0 — is the anchor, and this run must land within 0.15 of it; the 2026-09-17 flake moved it 2.9 (57.5 to 54.6, the figure that scene reads with `scene.environment` removed). Nothing is pinned: the anchor is whatever the last run of the same source measured, so an iteration lane that changes the scene resets it and nothing has to be maintained. Its bounds are real and printed: it needs a previous anchor OF THE SAME TREE, so a fresh clone and every CI run skip it and say which case they took — **and it would NOT have caught the 2026-09-17 incident itself**, which was the first `npm test` on a freshly merged main with no same-tree anchor; it catches every repeat. It says the two runs disagree and never which is right. The tolerance is 0.15, against a measured run-to-run jitter of ZERO over the 54 logged shot runs that recorded it, so it is one rounding step of a one-decimal figure and a twentieth of the 2.9 the defect moved. The anchor lives in its own file, written before the comparison decides anything, because keeping it in `out/scores.json` — which `compare` writes only after the check passes — would let one flake wedge every later correct run red against a stale figure. And it is blind to any change that moves no light. It writes `out/compare.png` (photo | render | 50% overlay | heat-map), `out/overlay.png` (the plan's landmark boxes over a 50% blend at 1200x1100), and `out/scores.json` (scores plus per-cell means, the tree the render came from, and the post-chain and environment state it was drawn at).
- `npm run try` asks **what does this tree score** and answers in seconds instead of a minute and a half, so an iteration lane can decide an A/B arm without the CPU rasterizer. It renders the same photo view at the same 1200x1100 with the clock pinned at t = 0, scores it with the same `cellDistance` and `ssimGray` out of `tools/lib/metrics.js`, and prints the two numbers, the renderer it got, the scene source tree hash, the frame's sha256, the post-chain rung and its own seconds split by phase. **It is a diagnostic, not a gate**: it is in no suite, it asserts nothing about the scene and it has no threshold — and it **writes neither `out/render.png` nor `out/render.tree.json`**, only `out/try/render.png` and `out/try/score.json`, so it cannot feed `compare` and cannot become the scored record. What it does refuse are instrument faults, because an A/B is void without them: a page error, a frame not drawn by the post chain as designed, **a frame that did not get the sky’s environment map** (`src/lighting.js` proves that map and this tool refuses an arm whose proof failed, on exactly the grounds it already refuses a lesser rung — losing the map costs this scene about 0.0036 of cell distance, **36 times this tool’s own margin**, so an arm that lost it would read as a scene change and is not one), and the scene source changing under the run. **Renderer: the GPU**; `TRY_GPU=0` (or `GATES_GPU=0`) forces SwiftShader, which is how an arm inside the margin is re-measured without leaving the tool. **Cost: measured INTERLEAVED, GPU arm and software arm back to back three times, because this machine's load swings enough that two arms timed minutes apart differ by more than the renderer — 14/85, 14/90, 15/88 seconds, median ratio 6.1x, and 6.6x against the 93 s `npm run shot && npm run compare` in the same block. Those seconds are upper bounds under a named load; the ratio is the claim.** The split is the reusable number: on the GPU the frame work is 0.4 s of a 14 s arm and the other 12.1 s is the scene BUILD, against 42.5 s of frame work in an 85 s software arm — so what is left to attack is a build every gate that opens the page already pays. **Agreement**: at `TRY_GPU=0` on the same tree its frame is byte-identical to `shot`'s (`e95a53185ee3…`) and its scores are equal to `compare`'s as doubles, not merely to four decimals. **The margin, printed every run: 0.0001 of cell distance and 0.0001 of SSIM.** Below it an arm is too close to call on the GPU and goes back to `shot`. It is calibrated against the GPU-versus-software crossing measured on four trees, not against run-to-run noise, which is zero on both renderers here (11 GPU runs one sha256, 3 software runs one sha256). Bounds, and the first is the big one: the calibration is one machine, one driver and four trees that are all **one scene varied by one constant**, so a second scene with another photograph and another rig is outside it entirely and the offset must be re-measured there; the margin covers comparing two arms from THIS tool with each other and **never** a `try` number against `docs/PLAN-scores.md`, since a single GPU score carries the whole crossing rather than its variation; and like `shot` it is one frame at one clock, blind to the animation, to every other camera and to anything 528 cell means and a 64 px image cannot see.
- `npm run record` drives the camera with real mouse events and checks every frame, failing when any one frame goes dark. It is the only gate that exercises the controls rather than posing the camera, and the only one that can see a flicker: the others sample moments. It also fails when it caught fewer than 30 frames, because too short a recording cannot tell a scene that never went dark from a recorder that never caught it. **Renderer: the GPU, which it has always asked for** — the fault it exists for is a driver behaviour, and it is the only gate whose assertions are about real time, so it is also the one gate whose verdict could move with machine load. `RECORD_SECONDS` and `RECORD_GPU=0` (or `GATES_GPU=0`) trim it and `RECORD=0` skips it with a printed reason; CI skips it, because on SwiftShader its 3007x1957 buffer had not finished after 19 minutes and would prove nothing about a GPU driver anyway.
- `npm run views` renders seven fixed poses to `out/views/` so an iteration is inspected from angles the scored frame cannot see; it is a diagnostic, not a gate. It poses the camera directly and does not exercise the controls, which is `npm run record`'s job, but it applies the same `clampCamera` a drag gets, and one pose exists only to prove that clamp fires. It found the top platform's bare face standing across the head of the stairs, which every scored gate passes over. There is no contact sheet: an aggregate view answers "is there one of each" and never "is each one right", so `out/views/index.txt` lists each pose's path, sha256 and mean luminance, plus one sweep digest over all seven to quote in a review, and the frames are reviewed one at a time at 1200x1100. **Renderer: the GPU, 11 s against 291 s**, because nothing here is a verdict — it fails only on a page error and its whole output is frames for a person to look at. **A sweep digest belongs to the renderer that drew it**: the same tree gives `b2e6a7395596` on the GPU and `beb2eef266b5` on SwiftShader, so a review quoting one is a review of that sweep, every digest quoted before 2026-09-15 is a SwiftShader digest, and the manifest names the renderer on its own line. **And a digest only binds anything if it reproduces.** Until 2026-09-16 pose 7's did not: the frame loop runs `controls.update(); clampCamera();` every frame, that pose is the one the clamp corrects, and the clamp and the controls' polar limit take six of those pairs to agree while the two settle frames give the page's loop an unfixed number of turns — so the bytes landed on one of three files run to run. Each pose is now settled to a bit-exact fixed point of that pair before it is shot, and a pose that will not converge in 32 iterations is warned about by name and marked in the manifest, because the warning scrolls past and the manifest is what a review quotes. It was never only pose 7. Pairs that move the camera, pose 1 to 7: **0, 0, 1, 10, 0, 2, 6** — four of seven were being rendered unconverged and pose 4 is the worst of them; only pose 7 crossed a pixel boundary, which is why only pose 7 was reported. **What binds the sweep to the score** is no longer that `1-photo.png` and `out/render.png` are byte-identical — they cannot be, since the two gates render on different rasterizers by design. The manifest now records the sha256 of the scene source these frames were drawn from (`index.html` plus `src/**`) beside each pose digest, `shot` records the same hash in `out/render.tree.json`, `compare` copies it into `out/scores.json` and refuses to score a render whose tree has moved, and `npm run treecheck` compares all three and fails when a sweep and a score are quoted as one tree and are not. Both tools read the hash before their first frame and again at the end, so "the tree held still" is checked rather than assumed. Elapsed seconds are printed per pose — every image operation after a pose is rendered runs on a second, blank page, because on the scene page under SwiftShader the render loop makes one `decodeImage` cost 80 s.
- `npm run treecheck` asks whether `out/render.tree.json`, `out/scores.json` and `out/views/index.txt` describe the same scene source tree, and the tree on disk, and names the files that differ when they do not. It opens no browser and costs under a second. **It is not in `npm test`**, on purpose: `views` is a diagnostic, a fresh clone's first suite run has never produced a sweep, and a gate that went red for a missing diagnostic would be a false red. Inside the suite it is `compare` that carries the same check for the half that always exists, failing when its sidecar is missing, when the sidecar's render digest is not the render on disk, or when the recorded tree is not the tree on disk. Bounds: it hashes `index.html` and `src/**` only, so a `tools/` edit or a CDN serving different three.js bytes moves no hash; **`japan.webp` is half of what `compare` scores and is bound by none of this** — it is in neither the hash nor `compare`'s mtime list, so a changed reference photo moves every score with no guard in this repo, and what binds it is its own bytes in git; it proves the artifacts agree about a tree and never that any of them is right; `docs/render.webp` is encoded from `out/render.png` without consulting the sidecar and so sits outside the binding too; and it reports a missing artifact as missing, prints how many pairings it actually made, and fails outright when no record carries a tree, so a run that compared nothing cannot read as a pass. **CRLF is normalised to LF before hashing**, because this repo has `core.autocrlf = true` and no `.gitattributes`: hashing raw bytes made the hash a property of the checkout rather than of the commit — the same tree read `3811005047643b3b…` here and `dda00820d3825d48…` with LF — so a hash quoted in a review meant nothing across machines and a Linux re-run against artifacts made on Windows gave a false red naming every file. The cost is that a pure line-ending change is invisible to it, which moves no pixel.
- `npm run perf` prints the median frame time and draw calls over 5 s at 1920x1080. Budget: under 16 ms and under 400 draw calls. It launches chromium with the GPU (ANGLE/D3D11) and prints the renderer it got; `PERF_GPU=0` or `GATES_GPU=0` forces SwiftShader. CI does not run it: a shared runner's frame time says nothing about the budget. Measured here: 4.90 ms median at 1920x1080 on ANGLE/D3D11, which is the number the whole renderer decision rests on.
- `npm run dev` serves the repo root on http://localhost:8080.
- `npm run probe -- u,v ...` names the mesh under each photo position and its pixel; `npm run inspect -- pair u0 v0 u1 v1` writes photo-versus-render crops. Both are diagnostics, not gates.
- `npm run shimmer` orbits the camera 0.1 degrees a frame through three poses at device pixel ratios 1 and 2 and scores the temporal second difference, `|2*F(i) - F(i-1) - F(i+1)|` as a frame mean and a worst 16x16 block, which cancels smooth motion and keeps pixels that toggle; a still control must read 0 or it exits 1, because a scene that changes with the camera and clock held still makes the figure meaningless. It is a diagnostic, not a gate: disocclusion at silhouettes sits under the figure as a floor, so the number compares builds on the same path and has no absolute threshold; it writes `out/shimmer.json`.

Bound of the compare scores: they see 528 mean colors and a 64 px grayscale image, so a landmark 10 px off in the photo is invisible to them. `out/overlay.png` is the check for placement.

## Invariants & boundaries

- The photo view is the contract. `src/layout.js` holds the camera model and every landmark's photo position, and the tools import it; changing the camera moves every placement and invalidates the thresholds.
- The post chain is proved, not assumed. `src/post.js` renders one real frame on build and on every size change and looks at it, because a multisampled half-float target of the wrong size turns the frame to NaN on some drivers with no error, no context loss and a complete framebuffer. If the frame is not there it steps down a ladder of configurations (fewer multisamples, a neighbouring size, no supersample, no multisampling) and says so in the console, and it starts again from the top at the next size so no machine is held to a lesser chain. `window.__scene.describe().post` reports what it settled on, and since 2026-09-17 it carries the watchdog's counters with it — `watch.floor` alone cannot answer "did the watchdog fire", because a ratchet followed by a re-tune back to an allowed rung leaves the floor where it was. **The watchdog's nine black pixels are a trigger and not a verdict**, since 2026-09-17: reading the default framebuffer back is not reliable (`tools/record.js` has said so for months), and one such read was ratcheting the chain down permanently in **1 of 25 fresh `npm run shot` runs on this machine**, drawing `1f74009907c0` where the contract is `e95a53185ee3`. A black reading now runs `verifyComposer` at the current configuration — which reads the composer's own targets and never the default framebuffer — and re-reads the nine pixels after it; only a bad target or a canvas that is still black steps down. A contradicted reading is counted in `watch.falseAlarms` and announced with `console.log`, NOT `console.warn`, because `shot` fails on a `post:` warning and a warning about a frame that turned out to be fine would move the defect rather than remove it. After three CONTRADICTED readings the watchdog switches itself off for that page and says so — and note what that does not cover, because the red proofs show it: a read that comes back black EVERY time is never contradicted, so it never reaches the give-up path and the chain walks the whole ladder down, which is the right answer for a canvas that is black and the wrong one for a driver that cannot be read. All five paths are red-proved by forcing the probe's answer and the verification's verdict (`docs/learning/gate-proofs.md`), and the field rate (1 of 25 before, 0 of 25 after) is reported there as too small to measure the fix — what carries it is the forced arm. **And the watchdog’s `renderer.info` restore leaves `frame` alone.** That field is not a statistic, it is three’s once-per-frame memo key for geometry, video-texture and UBO uploads, so restoring it wholesale rolled the counter back and could let the next few real frames skip an upload in silence. Found by an independent review of the same mistake written into `src/lighting.js` on 2026-09-17, and fixed in both. **A step-down was announced and nobody was listening.** `src/post.js` says so with `console.warn` and `collectErrors` only ever looked at `console.error`, so the scene could be drawn at a lesser configuration on any gate's page and every gate would report a pass and print nothing. Every gate now echoes the page's `post:` warnings as they arrive (`tools/lib/browser.js`), and `shot` fails on one: the scored frame is the full chain or it is not the contract. The same file echoes `env:` since later that day, for `src/lighting.js`’s proof of the environment map.
- **The environment map is proved, not assumed** — the same rule as the post chain, and it landed on 2026-09-17 for the same reason. `src/lighting.js` builds an image-based light out of the sky dome and hands it to every lit material, and until then it built that map and never looked at it. That is the hole the unexplained flake of that day fell through: removing the environment puts **416 of 528 cells on the bad frame exactly**, against 63 for the good frame and 68 for the next best of twelve scene knobs. It now measures four things after building it and reports them through `window.__scene.describe().env`: the radiance the map carries (mean luma **115.6** on a GPU, **117.6** on SwiftShader, **0** for a black one), the shape three compiles each program's cube-UV lookup from, which environment each lit program was built against (239 of 256 standard materials have a program at the photo view and all 239 agree; three writes that field only when it builds a program and never tests it for invalidation, so it is a cheap direct read whose natural case is covered by the identity check beside it), and **what the map actually adds to the photo view** at 64x64 with no post chain — 2.69 luma levels on SwiftShader and 2.64 on the GPU, against a floor of `max(0.25, 4 x RIG.envIntensity)`. That last one is the end-to-end number, and it is the same size as the 2.9 that separates the flake's frame from the contract's. **The contribution is measured through `scene.environmentIntensity` and never through `scene.environment`**, measured rather than assumed: the intensity is a uniform and costs 9 ms on a GPU and 428 ms on SwiftShader, while clearing the environment recompiles 18 programs and costs 1.2 s and 1.7 s, and the two answers differ by 0.02 of a luma level. **A bad MAP is built once more** before it is installed, because the dome is deterministic and a second build is a real remedy for a transient; a map that is fine and whose light does not arrive is reported and NOT rebuilt, because rebuilding cannot fix a lookup or a binding. **Nothing throws and nothing is switched off**: `npm run shot` refuses the frame and `npm run compare` refuses the sidecar, which is the split `src/post.js` settled on for its watchdog. **One scene is required to carry the block, not every scene**: the proof is scene 1’s rig, a second scene has its own rig under `src/` and its own `describe()`, and demanding it everywhere made `SCENE=whitehouse npm test` an unconditional red — a scene that reports a block is checked whatever scene it is. Bounds, and they are real: the map and the contribution are measured ONCE, at build, so a map that goes black afterwards is invisible and only the cheap half — the texture's identity, its shape, the intensity and the per-material binding — is re-read at the shot, so a mechanism that loses the light AFTER the build without moving any of those four reads as proved and scores. **The mechanism of the 2026-09-17 frame is still unknown, so nothing here says that frame would have been caught** — only that a map which fails at build now is; the shape check covers a height of **0** and a shape that CHANGES after the build, and a wrong height baked in at build is caught by nothing, measured on SwiftShader (at height 4 the lookup reads the wrong texels, the contribution reads 3.27 against that renderer's healthy 2.69, and every check passes); and `src/lighting.js` announces a failure with `console.warn('env: ...')`, which **every gate echoes** — `tools/lib/browser.js` gained `env:` beside `post:` and `watchdog:` the same day, because shipping a warning nothing listens to would have recreated, for the light, the defect this repo had already paid to find for the post chain. `shot`, `compare` and `try` refuse on the STATE; the line is what a person reads. The measurements are in `docs/devlog/detailed/2026-09-17-env-proof.md`.
- The frame is lit and tone mapped from phase 4: `src/main.js` builds the rig (`lighting.js`) and the post chain (`post.js`), and every color in `src/layout.js` is a *displayed* target, not an albedo. `src/tonemap.js` inverts the ACES curve three applies, and `materials.js` turns each sampled mean into the albedo that displays as that mean again through the rig and the curve. Add a color the same way: sample it from the photo, hand it to `material()` or `surface()`, and let the factory do the inversion.
- `index.html` is the only page. `src/boot.js` reads `?scene=<id>`, sets the tab title, mounts the picker (`src/picker.js`) and imports that scene's entry module named by the registry (`src/scenes.js`); an unknown or missing id loads the first entry. Scene 1's entry is `src/main.js` and its modules sit flat in `src/`, from when it was the only scene; a new scene is a registry entry plus its own folder under `src/`. Switching scenes reloads the page rather than swapping in place, so no scene has to know how to tear itself down.
- The page's chrome lives in one `#hud` box, because the gates that screenshot hide it wholesale (`HIDE_UI_CSS` in `tools/lib/browser.js`): a control added outside that box would land in the scored image.
- The scene is assembled by `src/scene.js` from modules: `paving.js` (instanced slabs: stairs, gutter, landing, street, side stair, platform), `walls.js` (stacked stones, the low plaster wall and its tiled cap, the fence's stone core, the pots), `roofs.js` (kawara pan tiles, caps, ridges, rafters, boards, the fence's small roof), `facades.js` (board walls, lattices, shoji, balcony, doors, awning, sign, lanterns, fence boards, lamp post, noren), `architecture.js` (what is still block-out: roof undersides, the right roof mass, the paved bands; plus the dormer's body), `vegetation.js` (the cherry: spline limbs, strands, blossom cards colored from the photo field; pines; shrubs, potted plants, moss, weeds), `figures.js` (the person), `animation.js` (the wind, the petals, the drift and the sway, and the clock the gates pin), `background.js` (mountain layers, the hill surface, the far houses and their ground, the bend's block-out). `boot.js` resolves the scene and `scenes.js`/`picker.js` list and switch them, `primitives.js` holds the mesh helpers, `instancing.js` the InstancedMesh and geometry helpers, `textures.js` the canvas albedo and card generators, `materials.js` the material factory (lit since phase 4; `MATERIALS.lit` selects the material class, not a whole unlit pipeline), `tonemap.js` the tone curve and its inverse, `lighting.js` the rig, `sky.js` the dome, `post.js` the composer, `photofield.js` the baked canopy field and mask, `random.js` the seeded PRNG.
- Repeated pieces are instanced geometry (one InstancedMesh per material): tiles, slats, slabs and stones are never painted on. The scene builds deterministically from fixed seeds, so the shot is reproducible.
- Every hand-built geometry must compute its normals. A missing normal attribute is invisible under an unlit material and produces NaN under a lit one, and one NaN texel spreads through the bloom blur to the whole frame.
- Photo space is (u right, v down) as fractions of the frame. World space is y up with the photo camera at x = 0 looking along -z; +x is the right side of the street.
- Nothing binary enters Git except `japan.webp` (the reference) and, in phase 5, `docs/render.webp` under 256 KiB. `out/` is ignored and regenerated by the gates.
- No runtime downloads except the pinned Three.js modules. No bundler, no build step.
- The integration owner maintains `docs/work/0_japan-street-scene/plan.md`. The original manager-authored plan at `docs/work/0_japan-street-scene/historical/PLAN.md` is preserved unchanged.

## Conventions

- Colors in `src/layout.js` are sRGB hex values sampled from the photo. Materials are unlit until phase 4; every procedural texture is corrected to its material's mean (`textures.js`), so textures add variation around the sampled color and never a new color. Where a lighter or darker element sits in front of a wall (joints, slats, shoji, mortar between stones), the wall's color is balanced (`balancedMean`) so the region still averages to the sampled mean. A few accents with no clean photo region (lattice slats, shoji paper, grid bars, the mortar and cap darkening factors) are set by eye and say so where they are defined. Normal and roughness maps are generated alongside each albedo and kept in `material.userData.pbr` for the lit materials of phase 4. Foliage is alpha-tested cards: the card textures are white shapes, and each instance's color is a sampled mean divided by the card's opaque-texel mean, so the visible mean lands on the sample. The cherry's blossoms take their colors from `src/photofield.js`, the photo's own cell means over the canopy, at their photo position; regenerate that field only if the photo changes.
- Anything the photo pins down is placed through the helpers: `uvToWorld` (a photo position at a depth), `rayHitGround` (drop a point onto a height field), `depthForU` (depth for a known side offset). Dimensions the photo cannot pin down (house depths, terrace lines, wall thicknesses) are block-out estimates typed into `src/layout.js`, or into the module that builds them, with the photo landmark they serve named nearby.
- Every mesh has a `name`; the probe tool maps a wrong pixel to its mesh.
- Anything that moves is animated in the vertex shader when there is more than a handful of it: `src/animation.js` patches a material's `project_vertex` with a wind offset and advances one time uniform, so 30,000 blossom cards and 420 petals cost no per-frame CPU. The clock is deterministic and `window.__scene.setTime(t)` pins it, which is how the gates stay reproducible.
