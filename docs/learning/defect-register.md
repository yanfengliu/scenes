# Defect register

Every defect the user reports is recorded here and gated, never only fixed. The entry stays after it becomes a gate: this is the standing list of what the gates could not see.

Entry shape: date, symptom as the user saw it, investigation, root cause, and how it is checked from now on (the gate covers the defect's whole class, not the one instance).

| Date | Symptom (as reported) | Investigation | Root cause | Checked from now on by |
| ---- | --------------------- | ------------- | ---------- | ---------------------- |
| 2026-09-06 | "when I move the camera around there are even giant rectangular blackouts on screen. It continues even after the camera stops moving. Only clicking the reset review button made it go away." | The camera is incidental: this reproduces with the camera stationary, from the size of the composer's targets alone, and the reset only ever appeared to help because whatever else changed alongside it changed the size. Measured on ANGLE/D3D11 (NVIDIA RTX 4090) at a 1280x720 window and a device pixel ratio of 1.5, so a 1920x1080 drawing buffer and a 2304x1296 composer: 96.6% of the frame is near black through the composer against 0.8% through `renderer.render(scene, camera)`. The frame is not black, it is **NaN**: read back as half floats, both of the composer's ping-pong targets are 100% NaN, and NaN reaches the canvas as black. Sweeping the composer's size from 1.00 to 1.60 times the drawing buffer in hundredths, 14 of the 61 sizes fail at 8 samples and 10 of 61 at 4, and the two sets only partly overlap; every size is deterministic to two decimal places over repeats and arriving from either direction, with `gl.getError()` 0, no context loss and FRAMEBUFFER_COMPLETE throughout. Turning the bloom pass off makes every bad size correct (1.96%); setting its strength to 0, its threshold to 1e6 or its radius to 0 does not, so the bloom spreads the NaN rather than making it. Setting the composer's targets to 0 samples makes every bad size correct. The scene is innocent: rendered into a fresh target of the same size, multisampled or not, it reads back with no NaN at all and a peak radiance of 1.65. Two explanations were tested and killed. A broken multisample *resolve*: a bright gradient painted through the very same target and sampled back comes back perfect at every bad size, and so does the whole chain run on a synthetic input, so only the real scene triggers it and no synthetic probe can stand in for the frame. And a buffer that could be routed around: giving the scene render its own multisampled target and leaving the composer's own buffers unmultisampled still produced the NaN, even though that scene target itself read back clean. | A multisampled half-float render target of certain sizes poisons the post chain on this driver, and the bloom's blur carries the resulting NaN across the whole frame. It is a property of the (size, sample count) pair and of nothing in the scene, no API call admits to it, and there is no rule about the sizes to be had: of every size measured, 2304x1296 is the only one whose width and height are both multiples of 16, so an alignment rule would steer toward the failures rather than away. | `npm run blackframe` (in `npm test`): six window size and device pixel ratio combinations including non-round ratios, each measured on load and again after a resize, failing when the frame is too dark, too flat, has a blacked-out tile of a 12x12 grid, or has lost most of the light the plain renderer sees. The fix is in `src/post.js`: on build and on every size change the chain renders one real frame, samples both of its buffers for anything that is not a finite number, weighs its brightness against the same scene rendered at 64x64 with no post chain, and steps down a ladder of configurations if the frame is not there, starting again from the top at the next size. Proof of the red run in `gate-proofs.md`. Note what did *not* see this: `npm run nudge`, the gate written for the previous report from the same user, scores the fraction of pixels that *change* between two renders of almost the same view. A frame that is entirely black is the most stable frame there is, so nudge scored this catastrophe as a flawless pass for its whole life. A stability metric cannot tell "rendered correctly" from "rendered nothing". |
| 2026-09-06 | "As I move the camera around it flickers a lot" | Two renders of an identical pose are byte-identical, so nothing is nondeterministic per frame and the animation is not involved. A 2 mm camera nudge changed 4.76% of pixels at the photo view (0.32% of them by more than 90 levels), 5.39% close to the paving (0.57%), 0.19% orbited (0.01%). The first hypothesis was depth fighting between the surfaces phase 2 stacked centimetres apart, and it was wrong: the composer's render targets carry 24-bit depth (measured, not assumed), and moving the camera's near from 0.2 to 1.0 or 2.0, or far from 6000 to 400, changed the photo view's figure not at all. Disabling bloom, and the whole post chain, changed nothing either. An amplified difference image settled it: the change sits on the edges of fine geometry and all through the canopy, with flat interiors untouched, which is not what a depth fight looks like. Then the remedies were measured: maximum anisotropy did nothing, alpha-to-coverage did nothing, and supersampling halved the drastic changes. Asking for eight multisamples produced byte-identical output, which was first written down as "SwiftShader does not honour the request"; the critic showed that was wrong. SwiftShader reports a maximum of four samples and WebGL clamps to it, so the request worked and simply could not go higher there; a desktop GPU reports eight and does use them. Multisampling turned out to be carrying more than half the stability, and switching it off at the shipped supersample doubles the drastic changes. The first fix was also switched off exactly where it was needed most: it capped its own scale against the device pixel ratio and sized its target from CSS pixels, so at a pixel ratio of 2 the scene rendered at a quarter of the canvas and scaled up, measuring worse than the state the user complained about, while the gate ran only at ratio 1 and saw nothing. | Geometric aliasing, not depth. The scene is full of geometry thinner than a pixel: the cherry's strand tubes are about 0.35 px wide at their distance, and so are tile ridges, lattice slats and the edges of some thirty thousand alpha-tested blossom cards. A triangle that lands between the samples either covers one or covers none, so a fraction-of-a-pixel camera move switches whole pixels on and off. Multisampling catches most of it and supersampling catches more; neither can help what falls between all of the samples. | `npm run nudge` (in `npm test`): three poses at two device pixel ratios, each rendered twice for an identical-frame check and then with the camera moved 2 mm, failing when too many pixels change by more than 90 levels. The limits are per ratio, because a ratio 2 pixel covers a quarter of the screen area and the fractions are not comparable. The fix is in `src/post.js`: eight requested multisamples (the driver clamps to its own maximum) and `POST.renderScale` at 1.2, so the scene renders above the drawing buffer and resolves down. Measured, fixed against the state the user saw: at ratio 1 the three poses go 0.15/0.00/0.28 against 0.32/0.01/0.57, and at ratio 2 0.26/0.00/0.50 against 0.40/0.00/0.70. Costs 0.6 ms of a 16 ms budget. Proof of the red run in `gate-proofs.md`. |
| 2026-09-07 | "the bug is still there" — a photograph of a real window: a hard-edged rectangle of flat dark filling part of the screen with the scene rendering correctly around it, after two earlier fixes had been called done | Every earlier tool sampled MOMENTS: one frame on load, one after a resize, one at a chosen pose. `tools/record.js` records EVERY frame while real mouse input drives the camera, its probe registered after the page own callback so each sample follows that frame render. Twelve seconds of ordinary gestures at 2005x1305 on a GPU: probes dark went 0, 15, 15, 0, 25, 15, 10, 15, 12, 25 on consecutive frames while the camera moved a tenth of a metre each. It was never a stuck state; it was a flicker, which is what the user said in their first message. A screenshot at one of those poses reproduced the photograph: a 2241x1406 block, 100% filled, RGB 0,0,0 — not the page background, so nothing was unpainted; it was rendered. Three measurements at that pose separate it from the size fault above: the chain own check reports 53.9% of the frame not a finite number; disabling the bloom pass alone takes the frame to 0.1%; and dropping multisampling does NOT help (54.4%), so the fallback ladder could never have reached it. A pass that strips non-finite values between the scene and the bloom changes nothing, so the bloom is handed finite input and makes NaN from it. | The bloom blurs in a half-float target whose largest value is 65504. Its bright pass and separable blur carry the sun radiance past that, to infinity and then to NaN in the composite, and the blur spreads each bad texel across its kernel and down its mip chain, turning a few pixels into a rectangle covering half the frame. Half the earlier confusion was that the two blackout faults look identical and have unrelated causes. | `npm run record` (in `npm test`): every frame of a scripted gesture sequence driven by real mouse events, failing when any single frame has 12 or more of 25 probes dark. Per FRAME, not averaged, because a flicker averages away into the good frames. Measured 25 of 25 worst and 89 heavy frames broken, against 3 of 25 worst and none heavy fixed. Red proof: with the ceiling pass removed, 171 of 1022 frames fail and it exits 1. The fix is a ceiling of 64 on what reaches the bloom, far above bloom own threshold of 1.6 and far below anything that survives tone mapping, written in GLSL 3 for real `isnan`/`isinf` because the GLSL 1 `x != x` trick is what a compiler may fold away. |
| 2026-09-17 | **Not reported by a user — found at integration.** `npm test` on main at `09eb339` failed its cell-distance threshold (0.0632 against 0.0611) on a tree that had scored 0.05958 in the worktree it came from. The scored frame `out/render.png` was sha256 `0e56fc677faf`; `npm run shot && npm run compare` on the same tree minutes later drew `e95a53185ee3` at 0.05958 / 0.57134, and a second full `npm test` drew `e95a53185ee3` again. Three good runs, one bad, one machine, the same renderer string, the same 363 draw calls and 714,090 triangles. | The hypothesis was a post-chain step-down. **It is wrong for that frame.** Forcing each of the five rungs available at 1200x1100 on SwiftShader through `shot`'s own path gives five different files, none of them the bad frame; every rung sits 0.24 to 0.86 levels from the good frame and 6.6 to 6.8 from the bad one, which is exactly where the good frame sits. What the bad frame IS: every lit surface darker by about 6.4%, the sky bit-identical, whole surfaces filled uniformly rather than edges moved. Twelve scene knobs were shot and each one's per-cell signature correlated against the flake's: removing `scene.environment` fits at **correlation 0.985**, and matches **416 of 528 cells exactly** against a baseline of 63 and a next-best knob of 68. **But running `npm run shot` as its own process 25 times over found a DIFFERENT flake, and that one reproduced**: run 18 of `out/scratch/shot-loop2.log` ratcheted the post chain's black-frame watchdog on a healthy frame and drew `1f74009907c0` at rung 1. `watchPostChain` reads nine pixels off the default framebuffer, and reading that buffer back is not reliable — `tools/record.js` has documented for months that it can hand back cleared black through no fault of the scene. Rate on this machine: **1 in 25, 4%**, and that run's `scene ready` was 14.5 s against 18.3–28.0 s for the other 24. | **Two causes, one fixed.** (1) The watchdog treated a single unreadable drawing-buffer read as proof and stepped the chain down permanently: reproduced, fixed, red-proved. (2) The `0e56fc677faf` frame is **not established**. Its measured signature is the loss of the environment-map contribution, and `env-null` still differs from it on 112 of 528 cells -- a residual of mean 0.038 of a luma level that sits entirely in rows 0-2 and is below the rounding of the recorded means over the other 86% of the frame. Removing the environment BEFORE the build (`out/scratch/env-absent.mjs`, through `page.route` so the tree does not move) gives the identical digests and the identical 416 of 528, so the residual is not a material recompile. 56 fresh `npm run shot` processes across three loops and 7 fresh pages failed to reproduce it; 54 of the 56 recorded a referenceLuma and every one read 57.5, and the pages recorded environment mean 176.309. | **The watchdog:** a black reading is now a trigger, not a verdict — `src/post.js` runs `verifyComposer` at the current configuration (which reads the composer's own targets, never the default framebuffer) and re-reads the nine pixels after it, and only steps down if the targets are bad or the canvas is still black; three contradicted readings switch the watchdog off for the page. **The frame:** `npm run shot` records the post-chain state in `out/render.tree.json` — rung, whether it is the chain as designed, scale, target size, samples, the `verifyComposer` verdict (badTaps, meanLuma, **referenceLuma**, lightKept) and the watchdog's counters — prints it, and fails on anything but the designed chain, a ratchet, a `post:` warning, or a configuration that moved mid-run; `compare` refuses the same and copies the block into `out/scores.json`. **The light:** `compare` holds `referenceLuma` to within 0.15 of the previous run OF THE SAME TREE (`out/light-anchor.json`), which is the figure that separates the unexplained frame (57.5 against 54.6) and which is anchored to the last run rather than pinned, so an iteration lane resets it; it cannot run on a fresh clone or on CI and would not have caught this incident, only its repeats. And every gate now echoes the page's `post:` warnings: `collectErrors` watched only `console.error`, so a step-down could happen on any gate's page and every gate would report a pass and print nothing. **And later the same day, the map itself:** `src/lighting.js` built that environment map and never looked at it, which is why no number about it existed. It now measures four things after building it and reports them through `describe().env` -- the radiance the map carries (mean luma 115.6 on a GPU, 117.6 on SwiftShader, 0 for a black one), the shape three compiles each program cube-UV lookup from, which environment each lit program was compiled against (239 of 256 have a program, 0 disagree), and **what the map actually adds to the photo view** at 64x64 (2.69 luma levels on SwiftShader, 2.64 on the GPU, against a floor of `max(0.25, 4 x RIG.envIntensity)`), which is the same quantity that separates the two frames. A bad MAP is built once more before it is installed; a map that is fine and whose light does not arrive is reported and not rebuilt. `npm run shot` refuses the frame and `npm run compare` refuses the sidecar, so a map that fails at BUILD is now red on its own run, which the same-tree light check could not be because that incident was the first run of a freshly merged tree. **Whether it would have caught this incident is unknown and is not claimed**: the map and the contribution are measured once, at build, and the mechanism is still unproven, so a fault that removes the term after the build reads as proved. **One sub-hypothesis is now dead:** a texture whose `image.height` is 0 makes three emit `-Infinity.0` into every lit program and 18 fragment shaders fail to compile, each a `console.error` that `shot` already fails on -- and the bad run had none, so it was not that. Bounds: the map and the contribution are measured once, at build; a wrong height baked in at build is caught by nothing (measured -- at height 4 the contribution reads 3.27 against a healthy 2.64 and every check passes); no gate echoes the `env:` warning, only the state, and only `shot` and `compare` read that state; and the binding read is of a field three writes when it builds a program and never tests for invalidation, so its natural case is covered by the identity check beside it rather than by the count. **THE MECHANISM IS STILL UNPROVEN AND THIS ENTRY STAYS OPEN.** A rebuilt hunt -- `npm run shot`'s own path, one fresh node process and one fresh chromium per run, recording the frame digest beside the whole of `describe().env` -- got **37 clean runs** on 2026-09-17 and **did not fire**: every one drew the contract frame `ef7a32617aa6` with the map at mean luma 117.6, image height 1024, 0 mismatched bindings and a contribution of 2.69. With the previous lane's 56 fresh `shot` processes that is 93 runs and no reproduction. What the proof adds is that the NEXT occurrence is diagnosable and refused rather than scored; what nobody has yet is a cause. Proofs in `gate-proofs.md`; measurements in `docs/devlog/detailed/2026-09-17-shot-rung.md` and `docs/devlog/detailed/2026-09-17-env-proof.md`. |
| 2026-09-17 (later) | **The White House scene: two sightings of one class, both of them a hole in the ground.** From the west orbit, seen and reported as an object: "a dark, slab-like object floating beside the building's west end" — it was the frame's left edge looking PAST the ground's west edge, and the boundary wall's own lit top edge cutting across the hole is what made it read as a fallen beam. From directly above and from the west: "a blue band across the grounds", the sky through a 40.6 m by 1040 m trench between the north lawn's south edge at z 0 and the south lawn's north edge at z -40.6. | Neither sighting was in the scored view, and the instrument that found them both was a downward-ray grid plus a top-down orthographic render, not a photograph comparison: `out/wh/scratch/whvoid.mjs` drops a ray on a 20 m grid over x +-560, z -360..+320 and prints a map whose blank cells are points where the ray leaves the world. It found 718 of 1995 cells blank in the trench. The scene's own scores file records what the scores did about it, measured: the pass that closed the trench "moved both scores the right way for unrelated reasons", because the trench lies behind the building from the photo view. **A metric can improve while the scene is broken somewhere the metric does not look.** After the trench was closed the same sweep still found 246 blank cells — the whole rim of its own window, 12 cells in each of 32 rows (six at each end, x +-540 to +-560) plus the two full rows at z +320 and z -360 — and rebuilt as the wider gate `tools/groundcover.js` (x +-640, z +-900 at 20 m, 5,915 cells) the same defect read **4,166 of 5,915 blank**: 12 cells in 32 rows plus every row past z -320 and past z +320. The instance the scored frame itself shows is the southern one: with the south slab still ending at z -340.6 the 1200x900 frame drew the ground ending in a hard straight edge with sky BELOW its own true horizon at its left and right edges. | The ground was never one surface. It is four slabs in `src/whitehouse/grounds.js` — the north lawn grid and the apron (one surface, z -40.6 to +91.9), the far ground (a box north of that), the south lawn (a box south of z -40.6) — and all of them stopped at `TERRAIN_HALF` = 520 m and at their own z ends. The x rim had been sized on the claim "+-520 m covers the frustum from any camera the clamp allows", and that claim is false: the clamp (`clampCamera` in `src/whitehouse/main.js`) caps the camera's orbit distance at 260 m and the frame's own half-width is 0.7213 of the distance to what it shows, so a camera at the clamp's own extreme reaches 634.6 m while showing ground at the 520 m edge. The z ends are not a sizing error at all — the slabs simply stop — and the south one is load-bearing, because that is where the frame's own horizon is drawn. | `npm run groundcover` (`tools/groundcover.js`; in `npm test` for `whitehouse`, and skipped for every other scene with its reason printed): a ray dropped from 400 m straight down on a 20 m grid over x +-640 m, z +-900 m must never reach the sky dome — **0 of 5,915 cells blank**, with a 5,000-cell floor so a sweep that measured nothing cannot read as a pass, and a floor of exactly one mesh named `sky`, because the blank test is written against that name. It measures the BUILT scene three actually draws and imports nothing from `src/`. Fixed in the same pass: `TERRAIN_HALF` 520 -> 680, the far ground 216 -> 850 m deep, the south lawn to z -940.6. The x and north extensions are byte-identical on the scored frame (sha256 `f1d1475a…`, scene tree `48117b34…` -> `de1fb1af…`); the south extension moves 2,185 pixels of 1,080,000 (rows 460-516, cols 54-154 and 1045-1083 either side of the building, max channel delta 93) and both scores improve: cell distance 0.09492570 -> 0.09490477, SSIM 0.42069753 -> 0.42077211, against thresholds 0.1061 / 0.4086. **What this gate still cannot see, and it is named in its own header:** a hole outside that box, a hole narrower than one cell (20 m), and a hole hidden under another mesh — and because `clampCamera` never reads x or z, a camera panned far enough can be put over ground that no finite box contains. Proof, with the mutation, in `gate-proofs.md`. |
| 2026-09-17 (deploy) | **Reported by the user: "The current deployed version fails."** The GitHub Pages site's White House scene did not load; the default scene did, and every workflow run on the remote was green. | The deploy job was not the failing part: `gh run list` showed the newest run anywhere at `ce3f335` with `conclusion: success` for both `pages` and `test`, and no run existed for any commit after it. Reading the artifact's own definition found it — `pages.yml`'s "Collect the site" step copied `index.html`, `japan.webp`, `README.md` and `src/*.js`, one flat glob, so the site contained scene 1 and nothing else while the copied `src/scenes.js` advertised a second scene. Reproduced by running that step's own shell (`bash -e`) into `_site`: the fixed step collects 42 files, and a copy built the old way is missing `src/whitehouse/main.js` and `whitehouse.webp`. The page's failure mechanism is `src/boot.js` line 12, `await import(scene.entry)`: the entry module is imported DYNAMICALLY, so the missing files are a 404 that only a visitor who picks that scene ever triggers — which is why a green deploy over a working default scene hid it for as long as the scene has existed. | **A deploy that copies a list of files is not a check that the site can load what the registry names.** The collection hard-coded scene 1's shape, and `src/*.js` does not descend into a scene's own folder — the layout AGENTS.md prescribes for every scene after the first — and no step read `src/scenes.js`, so the artifact and the registry could disagree indefinitely with every run green. The class is "the artifact is incomplete and nothing asserts that it is not", not "somebody forgot a file". | `tools/sitecheck.js`, run in the `pages` workflow between the collection step and the upload: it reads `SCENES` out of `src/scenes.js` and requires `index.html` plus every scene's `entry` (resolved from `src/`, the way the page resolves it) and `photo` to exist in the collected directory. Red-proved on the real artifact shape before the fix — the old collection exits 1 naming exactly `src/whitehouse/main.js (whitehouse entry)` and `whitehouse.webp (whitehouse photo)`, the fixed one exits 0 at 5 of 5 files — and its bound is printed with every run: it proves the artifact carries what the registry names, not that a scene loads, because it follows no imports and loads no page. The collection step also copies `src/.` whole and every `*.webp`, with each optional copy guarded so an absent optional file cannot fail the deploy. |
