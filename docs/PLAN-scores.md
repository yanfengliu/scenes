# PLAN scores (implementer-owned)

`npm test` reads the thresholds from the JSON block below and fails when a score is worse. At the end of each phase the implementer sets the thresholds to the achieved values minus a small margin and records the phase's numbers in the history table. Thresholds only tighten; a worse score is a regression to fix, not a threshold to loosen.

Scores are printed by `npm run compare` (see `tools/lib/metrics.js` for what each one can and cannot see):

- `cellDistanceMax`: mean color distance over a 24x22 grid of cells between `japan.webp` and `out/render.png`, 0 to 1, lower is better.
- `ssimMin`: grayscale SSIM at 64 px wide, -1 to 1, higher is better.

The margin covers rasterization differences between machines (anti-aliasing, SwiftShader versions); the same machine reproduces the numbers exactly. From phase 4 the frame also goes through a tone curve and a bloom pass, so a driver that rounds differently moves the scores a little more than before; the margin is 0.0015 on the cell distance and 0.005 on SSIM.

```json
{
  "cellDistanceMax": 0.0611,
  "ssimMin": 0.5663
}
```

Two questions the thresholds cannot answer are asked elsewhere. `npm run nudge` moves the camera 2 mm from three poses at two device pixel ratios and bounds how much of the frame may change drastically, because a still camera renders the same frame every time and the user's flicker lived entirely in the movement.

A third, added 2026-09-06: every scored gate here shoots one viewport at one device pixel ratio, and the post chain can hand back a black frame from the *size* alone. `npm run blackframe` loads the scene at six window size and ratio combinations, including non-round ratios, measures each on load and after a resize, and fails when the frame is too dark, has a blacked-out tile, is flat, or has lost most of the light the plain renderer sees. Nudge could not see it and never could: it scores how much the frame *changes*, and a black frame is the most stable frame there is.

The animation adds a third: the shot is one frame, and the scene moves. `npm run animation` scores seven frames across the wind's period and asserts the worst of them against these same thresholds plus 0.0006 of cell distance and 0.0025 of SSIM (this line said 0.002 and 0.006 until iteration 4; the tool and AGENTS.md have always used the smaller pair). The spread it measures is in the phase 5 devlog.

## History

| Phase | Date | Cell distance | SSIM | Draw calls | Median frame (GPU) | Notes |
| ----- | ---- | ------------- | ---- | ---------- | ------------------ | ----- |
| 1 (first block-out) | 2026-09-05 | 0.1438 | 0.1368 | 81 | | first render of the block-out, before any tuning |
| 1 (before critic) | 2026-09-05 | 0.0940 | 0.2929 | | | camera and every landmark tuned with the overlay and the per-cell ranking |
| 1 (done) | 2026-09-05 | 0.0941 | 0.2950 | 128 | 1.10 ms | after the critic's fixes (pot and side steps unhidden, shrubs and strands to plan, flat landing, hill gradient grid) |
| 2 (paving only) | 2026-09-05 | 0.0936 | 0.2969 | | | instanced slabs on stairs, gutter, landing, street, side stair, platform |
| 2 (all modules, before fixes) | 2026-09-05 | 0.0977 | 0.3083 | | | roofs whose slope runs toward +x had their tiles under the board (normal flipped) |
| 2 (before critic) | 2026-09-05 | 0.0899 | 0.3187 | 185 | 1.80 ms | stone, kawara, wood, lattices, noren; right eave stepping down the street; house 1 eave at 6.6 m |
| 2 (critic 1 fixes, first pass) | 2026-09-05 | 0.0942 | 0.2963 | 183 | | the placement fixes removed the canopy's light near section, raised the annex wall above the canopy, hung a lantern in a dark cell and dropped the corner block |
| 2 (after critic 1) | 2026-09-05 | 0.0903 | 0.3190 | 200 | 1.00 ms | canopy restored with the awning as a sheet on it, annex wall kept under the canopy, a dormer in the corner, low wall cap flattened, house 3 wall set back under its eave, stones proud of their mortar |
| 2 (after critic 2) | 2026-09-05 | 0.0901 | 0.3270 | 202 | 1.00 ms | sign on a bracket at house 3's corner, awning clear of the tile crests, dormer a wedge on the mezzanine, the wall notch faced in stone, gutter mortar stepped, a lattice window under the awning |
| 2 (done, after critic 3) | 2026-09-05 | 0.0899 | 0.3273 | 203 | 1.0 to 1.5 ms | fence roof corners clipped on one signed diagonal each (hips and valleys, boards cut the same way), walkway band behind the notch stones, window rail under the canopy, curbs on the mortar; a post under house 1's overhang was tried and cost 0.008 of SSIM, so it is not there |
| 3 (first cherry) | 2026-09-05 | 0.0921 | 0.3642 | 196 (shot) | | the block-out replaced: spline limbs, 640 strands, blossom cards colored from the photo's canopy field, pines, far houses, hill surface, mountain layers, person; the pines too tall and the canopy's sun-lit top missing from the mask |
| 3 (mask and pines fixed) | 2026-09-05 | 0.0853 | 0.4176 | | | sun-lit top and lavender strand ends counted as blossom, pines lowered, the annex's near door red and clear of the stone wall's end |
| 3 (before critic) | 2026-09-05 | 0.0855 | 0.4160 | 204 | 2.6 ms | the lamp post and trunk kept clear of blossoms and stems, the hill textured with cellular tree crowns; placement gate on 18 landmarks in npm test |
| 3 (critic fixes, first pass) | 2026-09-05 | 0.0908 | 0.3469 | | | the new hillside band rose into the mountains' rows; the pines moved to the hillside beyond the bend, limbs and stems clipped, ground under the far houses |
| 3 (after critic 1) | 2026-09-05 | 0.0854 | 0.4017 | 208 | 2.8 ms | hillside kept short, limb tubes ending inside the blossom, strand-end cells pink at their own lightness, blossom hues clamped to pink, tube winding outward, every material through the factory, a far-street check added (19 landmarks) |
| 3 (done, after critic 2) | 2026-09-05 | 0.0844 | 0.4159 | 211 | 1.3 ms | ground strips out of the frame, the far houses off the paving with dark walls below their gables, the shrub standing on the walkway behind the fence's jog, the pines' whorls down to the ground |
| 4 (lit, first pass) | 2026-09-05 | 0.4122 | 0.0287 | | | the tone curve inverted with the wrong constant (three divides by 0.6, the port multiplied), so every color came out washed |
| 4 (curve fixed) | 2026-09-05 | 0.1158 | 0.3719 | | | the rig's directionality overwriting the photo's baked shading: up-facing paving blue, camera-facing walls dark |
| 4 (rig rebalanced) | 2026-09-05 | 0.0976 | 0.3904 | | | most of the irradiance moved to a neutral ambient, the hemisphere's tint cut to 0.14 |
| 4 (glare tamed) | 2026-09-05 | 0.0841 | 0.4187 | 316 | 5.3 ms | bloom from 0.24/1.05/0.6 to 0.10/1.6/0.35: the first settings washed the hill beside the sun and cost 0.013 alone |
| 4 (before critic) | 2026-09-05 | 0.0825 | 0.4301 | 316 | 3.4 ms | backlit translucency through the blossoms, a limb over the machiya's eave filling the photo's canopy at u 0.75-0.83, finer hill crowns |
| 4 (done, after critic) | 2026-09-05 | 0.0820 | 0.4338 | 316 | 2.8 ms | the rim term's sign fixed so its main lobe fires, the ridge's glare confined to the sun's own column, the cirrus thresholds lowered until the clouds show in the scored view, the fog color inverted like every other mean, black fringes lifted off the alpha-tested cards |
| 5 (animated) | 2026-09-05 | 0.0820 | 0.4340 | 318 | 3.7 ms | wind through the canopy, petals, cloud drift, lantern sway, all in the vertex shaders |
| 5 (sky blue) | 2026-09-05 | 0.0816 | 0.4338 | | | `skyTopBlue` replaced by the photo's own top row (#9bc4e4) |
| 5 (before critic) | 2026-09-05 | 0.0812 | 0.4389 | 318 | 3.7 ms | the landing shading into the bend as the photo does |
| 5 (after critic) | 2026-09-06 | 0.0813 | 0.4385 | 316 | 2.6 ms | the animation gate given a motion bound that a fourteen-fold sway fails, the swaying strands and petals taken out of the shadow pass (they cast still shadows), CI trimmed to three frames; animation spread over 7 frames 0.0001 cell and 0.0005 SSIM, motion 0.18 levels |
| 5 (done, user's flicker fixed) | 2026-09-06 | 0.0811 | 0.4411 | 316 | 3.2 ms | eight requested multisamples and a 1.2x supersample resolved down, for the shimmer the user reported while orbiting; the scores moved because the same resolve runs in the scored shot, which is what the page does too |
| loop iteration 1 (before critic) | 2026-09-10 | 0.0769 | 0.4548 | 317 | | the machiya's plinth and house 1's eave split near/far, house 1's top roof lowered 17 cm off the photo's sky, the mezzanine and dormer darkened, the annex roof's tiles warm instead of blue-grey, the far houses' lower walls resampled, a plaster band added along the near house front |
| loop iteration 1 (done, after critic) | 2026-09-10 | 0.0768 | 0.4559 | 317 | 6.8 ms | the near eave's rafters lightened (the critic found the split's boundary is a diagonal in photo space, so the cell it was meant to fix was rafters, not fascia), the plaster band moved off the annex window's top rail and out of the awning check's 1.4 cm margin; thresholds tightened from 0.0825/0.436. Draw calls are the shot's 317; `npm run perf` reports 325 at 1920x1080 and 6.8 ms median, measured with about 40 other node and chromium processes on the machine, so that time is an upper bound and not comparable with the earlier rows |
| loop iteration 2 (before critic) | 2026-09-10 | 0.0754 | 0.4672 | 328 | | the machiya row that lines the right side of the far street, the landing repainted across the street, the lamp post re-read off the photo, and the platform's retaining wall unburied at the head of the stairs |
| loop iteration 2 (done, after critic) | 2026-09-10 | 0.0748 | 0.4753 | 326 | 6.1 ms | the critic found the landing's two slab sets reading as a sawtooth tear and an unmeasured PRNG reseed of two walls in the photo view: the landing became one set with a per-slab ramp across the street, and the platform wall went back to its original place with its new courses on a PRNG of their own. Thresholds tightened from 0.0783/0.4509. Draw calls are the shot's 326; `npm run perf` reports 336 at 1920x1080 |
| loop iteration 2 (fixups, shipped) | 2026-09-10 | 0.0749 | 0.4733 | | | two defects the scored frame cannot see, both costing it a little on purpose. A world-space 2.4 m headroom stops blossoms hanging into the street: the photo's blossom mask is a 2D silhouette, so it cannot bound how low a strand falls, and the canopy reached 1.71 m over the landing where a standing eye is 1.70 m. And `farRowFrontX` now floors against the street's centre line, because the photo's building edge on a street that bends more slowly put the row 0.46 m into the road by its last bay. The headroom costs 0.0013 of SSIM and the floor 0.0007. Thresholds stay at 0.0763/0.4703 (they only tighten), which leaves the SSIM margin at 0.0030 against a measured cross-machine noise floor of 0.0010 — thin, so a CI failure on that line is the threshold and not a regression; `HEADROOM = 2.2` scores 0.4744 and is the dial |
| loop iteration 3 (before critic) | 2026-09-15 | 0.0657 | 0.5291 | 341 | | the right half and the bottom third. The right walkway was a flat plane where the photo has flagstones and carried the frame's largest single error mass (1.94 over 20 cells at a mean of 0.0969, with the photo's mean and the render's five levels apart): it is paved now, with the ramp measured down the street rather than across it. The far koshi lattice split at 2.1 m into the two populations its six cells actually are; the side steps' tint ramp was running backwards against the photo; the annex wall split near/far and its koshi window given the photo's warm lattice instead of the scene's generic near-black; the annex roof's last two metres darkened per tile; the machiya's far plinth darkened. Both iteration-1 survivors closed: the annex canopy cell 0.2158 to 0.1394, and the shrub cell 0.2136 to 0.1914 by giving the near fence the foliage the photo shows over it. The far row has shopfronts. About 0.007 of the cell gain and 0.028 of the SSIM is the previous owner's inherited diff, re-measured here. Thresholds tightened from 0.0763/0.4703 |
| loop iteration 3 (done, after critic) | 2026-09-16 | 0.0660 | 0.5286 | 341 | | the critic found the walkway's near shadow was a half-plane and not a band, and that its 0.45 m ramp was narrower than the 0.62 m slab row pitch, so no row ever took an intermediate value and 32% of the walkway went near-black in one step at a slab joint -- invisible to every scored gate and plain in `npm run views` pose 4. The band now has a soft near edge over 1.2 m and the same measured far edge, so the cells reproduce to the digit. It also found the last slab row laid at 78 degrees across the terrace's own 2.3 m drop with a 0.27 m hole in front of it, the far row's corner posts standing 5 cm proud of the wall line where the comment claimed 2, `tileRoof`'s new per-tile colour hook handed the MUTATED point for eave caps, and the small pot 5 mm inside the new flagstones with the grounding check that exists to catch burial blind to it by prefix match. All fixed. It also showed that the walkway's gain is not even across the grid: the v 0.932 row went 0.662 to 0.125 and the v 0.977 row 0.345 to 0.568, because one slab row fills both and the photo wants one light and the other dark. On a second round it measured what this rig can do and found the walkway's near shadow painted at a linear 0.19 against the lit stone, removing 81% of the light, where the deepest CAST shadow this rig can throw removes 14% -- so the scene was rendering real shadows next to a painted one five times deeper, and pose 4 stepped 80 levels of luma across a single slab joint. It is 0x47525d now, a linear 0.60, over 1.24 m instead of 4.96, and the corner term that fired only on rows in front of the photo frame's own bottom edge is gone. That costs 0.0004 of each score and is taken deliberately, the way iteration 2 took 0.0013 of SSIM for the blossom headroom; narrowing the band moved zero of 528 cells, which is the evidence that everything in front of the frame is free. Thresholds tightened from 0.0763/0.4703; the SSIM margin is a full 0.005 again after iteration 2 shipped on 0.0030 |
| loop iteration 4 (done, after critic) | 2026-09-16 | 0.0640 | 0.5403 | 364 | 5.00 ms | the sky, the hill, the ranges, the valley, the left row's backs, the machiya's back and the bend. `out/scratch/bymesh.mjs` was rebuilt and put the SKY first, 4.208 of summed cell distance over 69 cells, which was not on the brief's list at all: its cirrus was a pink-grey read off cells that mix cloud with sky, its cumulus falloff still carried a quarter of its pink at the photo's own top row, and every cloud colour was a function of the noise rather than of where the sun is. Clouds now take their warmth from the sun and the sky's 69 cells fall to 3.729. Two things were believed and are false: `makeHillTexture` evaluated its glare Gaussian in TEXTURE u against a photo-u centre, putting the band at photo u 1.095, so the three upper stops were mis-placed -- a warm wash at the frame's right edge and a glare averaging 0.081 where the photo's own is (an earlier draft of this row said "dead constants" and "about 0.01", which a critic measured as wrong by 17x at the cell it matters most); and cell (0.146, 0.386), listed since iteration 1 as house 1's timber, is a HOLE -- `npm run probe` hits `near ridge` 1004 m away and the "greenish" is `nearRidge` itself. The hill's skyline was re-read column by column from the photo and is trees cut out of the texture's alpha now; the ranges run right under it instead of ending in mid-sky, with haze gradients; `ground base`, a flat fogged slab that filled most of poses 2 and 6, is a height-fielded hazed valley that moved zero of 528 cells by an exact photo-row ceiling. The far street's paving ran 7.5 m through the corner house, which is what `npm run clearance`'s 40-row exemption was for: it ends at the house's own face now and the gate reports 3 rows against 34, one of them blocked, by an eave. Thresholds tightened from 0.0675/0.5236. The critic retracted three claims and they are the ones to read: a control for `outer ground` measured on an arm that was then reworked four times (what shipped costs 0.0004 of SSIM, not zero), a colour fit for the hill that FAILED at two of the three cells it was solved against (its six cells are 0.025 worse than base and the gain is the tree line), and a clamp in `carryRight` that never fires on any shipped point, so a paragraph of parallax arithmetic defended constants nothing uses. It also found a real defect -- the machiya back's timber frame standing proud of a parapet over the 7.5 m where there is no upper storey -- and two sweep artefacts the fixes themselves created, a range card past the hill's own right end and the hill's left end as a vertical wall, both closed. A second critic round then found a regression the first round's own fix had caused: renaming two instanced sets off the `far roof` prefix took them out of `NO_CAST` in src/lighting.js, which is also a name regex, and put 37 instances into the shadow pass -- the whole unexplained 364 to 366 draw-call move, and 8.00 ms of frame time against 5.00 once `far house` was added to that list. A mesh name is load-bearing in two regexes that do not know about each other; the lesson is in docs/learning/lessons.md with the gate that retires it |
| loop iteration 5 (done, after critic) | 2026-09-17 | 0.0596 | 0.5713 | 363 | ~6.0 ms | the bottom third by measurement. The frame's two worst cells for three iterations, (0.813, 0.977) at 0.2673 and (0.854, 0.977) at 0.2484, are a STEP: on the right terrace every cell flips from lit stone to near black between z = -5.27 and -4.86, right across the terrace's width, and a world-horizontal line at z = -5.05 projects to v = 0.955 at every x, which is the cell boundary the photo flips on. `RIGHT_STEP` builds it, 0.40 m with its riser's foot just past the frame's bottom edge, jogging 0.27 m nearer past x = 2.78 because the photo's outer half is a mid slate and not black; those two cells are 0.0082 and 0.0187. Three things had to be right before it moved a pixel and two were wrong first, in the devlog. The wall-top strip is two populations across x (dark inside x 1.78, light outside), so it has a coping 0.37 m wide and not 0.80. The fence's wall was stone where the photo has a dark earth-plastered panel on a light plinth, measured down the wall's own face, and the bed under it was 0.45 m too high. The top eave's edge was 0.033 of frame high and only the EDGE moves -- dropping `roofY` instead scores 0.0643. `rightUpperBand` was a box 0.045 of frame tall over a strip 0.020 tall. The noren's far panel is hitched up because the hem is linear in z and both whole-cloth fixes cost more than they paid. The hill's glare was still 0.19 at u 0.729 where the photo is grey-green. The cut-stone and rubble textures' period is 2.4 m, not 1.4 and 1.0, which is the repeat the sweep has reported since iteration 2. Cost-neutral: 372 draw calls at 1920x1080 before and after and 4,592 triangles fewer. The frame time is about 6 ms on both trees -- 6.30 on the base and 6.00, 6.70, 5.90 over three runs on the shipped one -- and is NOT a speedup; a 4.40 ms reading taken two steps earlier is in the devlog as a retraction. Thresholds tightened from 0.0655/0.5353. The critic found five defects and they are the ones to read: the step riser's body was **0.0046 m deep, not 0.12** (`slabGeometry`'s local y span is 0.038 and the scale component multiplies it, where the two real risers in that file use a unit box), and the `right walkway` band stood 0.076 m proud of the lower flagstones through it; that band's "0.15 m behind the step" offset was a branch that NEVER FIRED, because both z values fell inside one `bandSolid` sample gap; the coping's own sampling box straddled the two populations the coping exists to separate, which is this iteration's whole thesis committed by the fix for it; its nearest stone was laid flat across the terrace's kink, 0.142 m proud; and `wallSkirt` had moved in the same edit as the bed and was never swept alone (swept now: 0.20/0.30/0.38/0.45 give 0.05993/0.05969/0.05958/0.05961 and 0.38 stands). Two numbers are retracted: four of the corner cells' "after" figures belonged to an earlier arm, and a 4.40 ms perf sample was a quiet-machine reading two steps before the shipped tree -- six runs spread 4.20 to 6.70 ms on one tree, so the draw-call count carries the cost-neutral claim and the frame time carries nothing |
