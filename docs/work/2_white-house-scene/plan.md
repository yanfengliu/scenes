# Work 2 — the White House scene

Status: open. Coordinator: the session that opened this entry. Implementation is delegated; the coordinator inspects renders and accepts handoffs.

## Outcome

A second scene in this repo: the White House (1600 Pennsylvania Avenue) and its grounds, built procedurally in Three.js in `src/whitehouse/`, registered in `src/scenes.js`, reachable as `?scene=whitehouse` and from the picker.

The scene's **photo view** reproduces a researched public-domain photograph of the building. The building is a real 3D model of real dimensions — its measured length, depth, storey heights, column order and count, window bays and portico geometry — not a painted backdrop, and it holds up from any orbit angle.

"Looks as realistic as possible" is the acceptance criterion, and it is judged the way this repo judges every scene: a render of the photo view scored against the reference photo (cell colour distance, grayscale SSIM), plus a multi-angle sweep that a person looks at, one frame at a time.

## Non-goals

- Photogrammetry, downloaded models, downloaded textures, any asset that is not procedural.
- A build step, a bundler, or a new runtime dependency.
- Touching scene 1. Its contract frame, its recorded digests, its thresholds and its modules stay exactly as they are. A second thread is working in scene 1 while this entry is open.
- Changing any shared module's behaviour for scene 1's benefit or this scene's.

## Acceptance criteria

1. `?scene=whitehouse` loads, renders, orbits, zooms, and resets to the photo view; `R` and the on-screen button both return to it.
2. The building's proportions are the researched real ones, cited in `src/whitehouse/layout.js` beside the figures, and a multi-angle sweep shows a coherent building from behind, above and to both sides — no missing face, no floating part, no inside-out surface.
3. `npm run shot` and `npm run compare` for the whitehouse scene produce a scored frame and sheets; the numbers are recorded in `docs/PLAN-scores.md` and asserted by `npm test`, with the thresholds set from the achieved scores minus a margin and never loosened afterwards.
4. Every gate in `npm test` still passes for scene 1, unchanged, with scene 1's scores unmoved.
5. The page reports no console error, no page error and no failed request at the photo view or in the sweep.
6. Every file this entry adds or edits is committed, and nothing else is. Verified with `git show --stat` on each commit.

## Reference photo

Chosen from the research report now at `docs/work/2_white-house-scene/research-photo.md`:

**`File:The White House June 2024.jpg`** — Wikimedia Commons, by DJTechYT, **CC BY-SA 4.0**, 3296x2472, a frontal view of the North Front from across Pennsylvania Avenue at the edge of Lafayette Square, taken at 13:08 on 20 June 2024.

Why it is the one: it is the only candidate that is simultaneously a full **frontal North Front** (not oblique), with the **North Lawn, the North Lawn fountain and the flower beds in frame** so the reference is a scene and not an elevation, shot at **midday in high summer** so the north facade is lit flat by sky alone with no harsh self-shadow to fight, and supplied with **complete EXIF** (GPS position and a 24 mm-equivalent focal length) so the camera can be reconstructed by arithmetic instead of by eye. It is also large enough to read the window pediments, the balustrade and the fence.

It is committed as `whitehouse.webp` (1200x900, 206 KiB, sha256 `6d10f85c2100…`), cropped to 4:3 and resampled from the original. The attribution and the share-alike term are recorded in `LICENSE`, beside the existing carve-out for `japan.webp`.

Three sources back it up and are cited where the model uses them: `File:Lado Norte de la Casa Blanca-2023 01.jpg` (CC BY 3.0, 7147x4541, 50 mm-equivalent) for facade detail, `File:North Façade White House.JPG` (public domain) as a licence-clean cross-check, and the HABS measured sheets for geometry.

The camera was calibrated against the photograph rather than taken from the EXIF, because the two disagree: a luminance profile down the frame puts the north wall's base at v = 0.617 and its parapet at v = 0.377, and the wall's left and right ends at u = 0.132 and u = 0.871, which fits a camera about 75 m north of the north facade at 1.6 m above grade with a vertical field of view near 52 degrees and a pitch of about +4.6 degrees up. The photograph's own EXIF compass heading disagrees with its coordinates by 19 degrees and was not used; the coordinates and the measured framing agree with each other.

The White House's real dimensions come from the White House Historical Association's published set, recorded with their sources in `src/whitehouse/layout.js`: 168 ft (51.2 m) long, 85 ft 6 in (26.1 m) deep without porticoes and 152 ft (46.3 m) with them, 60 ft 4 in (18.4 m) to the top of the roof on the north side and a north facade of 50 ft 4 in (15.3 m) from grade to parapet.

## Architecture

- **One folder per scene.** `src/whitehouse/` holds everything this scene needs: `layout.js` (camera model, photo landmarks, real dimensions, sampled colours, placement and grounding checks), `main.js` (the entry module: renderer, controls, camera, the `window.__scene` API), and one module per group of geometry.
- **The photo space and world space conventions are scene 1's**, because the tools and the gates already read them: `u` right and `v` down as fractions of the frame; world space `y` up with the photo camera at the origin looking along `-z`. The projection helpers are re-implemented in this scene's `layout.js` rather than imported, because the shared ones take the frame's aspect ratio from scene 1's photo; the divergence and the reason are recorded where they happen.
- **Shared modules are imported, not copied**, where they are scene-agnostic: `primitives.js` (the mesh builder and its normal computation), `materials.js` and `tonemap.js` (albedo inversion), `lighting.js`, `post.js`, `sky.js`, `instancing.js`, `textures.js`, `random.js`. The contract report at `out/wh/contract.md` records which of these are genuinely scene-agnostic and which are not.
- **`window.__scene` is the scene's contract with the tools**, and it is the same shape scene 1 exposes. Importing a scene must render nothing and start nothing; the entry module builds the scene, renders frames, and resolves `window.__sceneReady` (already set up by `index.html`) after the first frames.
- **Nothing about the scene is decided by taste where a photograph or a published dimension can decide it.** Colours are sampled from the reference photo; dimensions carry their source.

## Scoping the render and score path

`shot`, `compare` and `test` read scene 1's `src/layout.js` for the reference photo path, the frame size and the landmark marks. This entry makes those tools scene-scoped: a scene id selects the photo, the viewport, the output paths and the thresholds, and the default is scene 1 so its behaviour and its recorded digests do not move. The tool edits are deliberately narrow and are listed here rather than left implicit:

| File | Change |
| ---- | ------ |
| `src/scenes.js` | one entry per scene carrying the facts the tools need (id, title, entry, photo path, photo and shot size, output directory) |
| `tools/lib/scene.js` (new) | resolves the active scene from `SCENE`, defaulting to scene 1 |
| `tools/shot.js` | viewport and output paths from the active scene |
| `tools/compare.js` | photo path and output paths from the active scene; the same three scores and the same sheets |
| `tools/test.js` | asserts each scene's thresholds; scene 1's assertions are unchanged; a gate that reads scene 1's own landmark lists is **skipped with its reason printed** when `SCENE` names another scene, and the verdict line names what was skipped |

Not touched: `src/layout.js`, `src/main.js`, `src/background.js`, `src/post.js`, `src/lighting.js`, `src/animation.js`, `src/scene.js`, `tools/placement.js`, `tools/namerules.js`, `tools/clearance.js`, `tools/animation.js`, `tools/views.js`, `tools/treecheck.js`, `docs/PLAN-scores.md`.

## What the model is built from

Three sources, in this order of authority.

**1. The measured drawings, read directly.** The Historic American Buildings Survey (HABS DC-37, LOC digital id `dc0402`) has 41 surviving measured sheets, all public domain ("No known restrictions on images made by the U.S. Government"), and they are at predictable URLs: `https://cdn.loc.gov/service/pnp/habshaer/dc/dc0400/dc0402/sheet/000NN{r,v}.jpg`, where `NN` is the sheet's position in the index, not its sheet number. The sheets this scene uses, with the URL that reaches each one:

| Sheet | What it is | URL suffix |
| ----- | ---------- | ---------- |
| 31 | North elevation, full (with the datum chain) | `00007v` |
| 32 | **West** elevation, full and dimensioned (with the datum chain) | `00008v` |
| 33 | South elevation, full and dimensioned | `00009v` |
| 34 | **East** elevation, full and dimensioned (with the datum chain) | `00010v` |
| 35 | South portico elevations | `00011v` |
| 51 | South portico | `00026v` |
| 76 | **North portico details: column and entablature; pilaster and entablature** | `00034v` |
| 78 | Typical ground-floor windows and doors | `00036v` |
| 79 | **North portico entrance** — the sheet draws the entrance, fanlight and surround, no steps | `00037v` |
| 82 | **Typical first-floor windows — all three window types** | `00040v` |
| 70, 71 | Ground- and first-floor axonometrics | `00032v`, `00033v` |
| 2, 3 | Site plan and landscape plan | `00002v`, `00003v` |
| 4, 5 | Ground- and first-floor plans | `00004v`, `00005v` |

Sheet 82 (drawn as `00040v`, "typical first-floor windows") is the one that settles the facade: it draws an unpedimented window, a **triangular**-pedimented window on console brackets, and a **segmental**-pedimented window, at `1/4" = 1'-0"`, and its windows read **six-over-six**. **Sheets 31, 32 and 34 carry the level datums** — the north, west and east elevations; the South elevation is sheet 33.

**2. The vertical levels, off the elevation datum chain (sheets 31, 32 and 34).** The chain as this entry's research record read it gives: ground floor at **-6'-8"**, first floor at **+10'-0"**, second floor at **+30'-0"**, cornice at **+64'-6"**, roof at **~+65'-6"**, with a dimension of **38'-0"** from the first floor to the roof. That is the storey structure the model uses, and it is the only measured storey data found anywhere: no published source gives floor levels.

**3. The reference photograph, for everything the drawings cannot say.** Where the drawings are silent — the projection of each portico, the portico and column dimensions, the coursing, the fence, the grounds, the trees — the model follows the photograph, and the code says so at the point it does.

**What is deliberately NOT modelled.** Several figures the brief originally carried are wrong or unsupported and are not in the model: the roof is a **flat deck** behind the balustrade, not a low hip; the **South Portico has no pediment** (it is flat-roofed, hexastyle, six Ionic columns on a rusticated podium with seven arched openings, reached by a double staircase of 21 risers per flight); the **North Portico is tetrastyle** with a triangular pediment and Ionic columns carrying a swag of roses between the volutes; there is no pediment sculpture (that is the Capitol's); the window pediments alternate but **which end starts with the triangle is not recorded anywhere** and is read off the photograph. The building is modelled in its **pre-October-2025** configuration, because the reference photograph is June 2024 and the East Wing was demolished later that year.

**Two corrections to this scene's own research record.** The sheet-index mapping in the table above is now the verified one: `out/wh/habs-index-verify.md` fetched all 41 indices and identified each sheet from its own drawing and its own printed `SHEET n OF 85`, and it settles the disagreement between the two research files in favour of `out/wh/research-dims.md`, whose index table was right on every row it shares with this one; `out/wh/habs-findings.md`'s mapping — and the `out/wh/habs/big_NN.jpg` copies named after it — was wrong, so `big_7.jpg` IS the north elevation and the north elevation lives at index 7 (`00007v`), not at the `00010v` this table used to give it. That pass verified indices **02-16, 18, 20, 24, 26, 31-34, 36-38 and 40** by reading the drawing itself, twelve of them with the printed sheet number read off the TIFF title block; it did **not** establish indices **17, 19, 21-23, 25 and 27-30** (fetched but not read as images, or still behind the LOC rate limit), nor 01, 35, 39 and 41, which it does not address — no sheet this table names falls in either unverified group, so every row above rests on a sheet that was read, and no row carries `not verified`. The mapping is settled, not every citation: a citation's sheet must still be checked **by content** before its index is trusted, which is why the two comments in this entry and in `src/whitehouse/grounds.js` that give the South elevation's datum chain as "sheet 12, index 00012" do not survive it — index 12 is sheet 37, the north elevation's east half, and carries no datum chain, while the South elevation is sheet 33 at `00009v` and sheet 34 is the East elevation, not the South. Separately, the "granite retaining wall about 18 in high flanking the lawn" that the old residual (f) quoted from `research-dims.md` §8.2/§8.8 is the **fence's own boundary stone base** on the west and east boundaries and along Pennsylvania Avenue (CLR p.364), not a north-south wall between the two fronts, and it was deliberately not used as one; it is modelled already, as `fence wall`.

**A datum conflict found by the same pass, recorded and not resolved.** The level datum chain was read at native resolution on the full elevations 31, 32 and 34 and all three print the same numbers: TOP OF ROOF **55'-2 1/2"**, PROMENADE **42'-0"**, THIRD FLOOR **38'-10"**, SECOND FLOOR **20'-11 1/2"**, FIRST FLOOR **0'-0"** and GROUND FLOOR **-12'-8 1/2"** (`out/wh/habs-verify/datum-index07*.png`, `datum-index08*.png`, `datum-index10*.png`). Source 2 above and `out/wh/habs-findings.md` §43-54 give a different chain — ground floor -6'-8", first floor +10'-0", second floor +30'-0", cornice +64'-6", roof at ~+65'-6", and 38'-0" from the first floor to the roof — and those numbers appear nowhere on the sheets this pass read at native resolution: the first-floor-to-roof figure there is 55'-2 1/2", the first-floor-to-promenade figure is 42'-0", and the second floor sits at +20'-11 1/2" rather than +30'-0". This is not the index question and is deliberately not resolved here, but the storey structure the model uses — the chain source 2 calls the only measured storey data found anywhere, which `src/whitehouse/layout.js` carries — may rest on a misread chain, so a pass that owns `src/` should re-read sheets 31, 32 and 34 before relying on it; the crops are in `out/wh/habs-verify/` for that pass to start from.

## What the gates can and cannot see here

- `shot` and `compare` see a 24x22 grid of mean colours and a 64 px grayscale image, so a window a few pixels off is invisible to them. The sweep and the overlay are the placement checks.
- Scene-scoped gates that are scene 1's by construction — `placement`, `clearance`, `namerules`, `animation` — are **not** extended to this scene in this entry. They read scene 1's landmark lists and its street, and running them against a second scene would report scene 1's expectations about a scene that is not scene 1. This scene gets its own placement and grounding checks in its own `layout.js` and its own tool; that bound is stated here rather than left to be discovered.

## Status log

Newest first. One line per event, with the evidence it rests on.

- **The storey chain reconciled against the sheets and NOT reconcilable — one new residual, and the first open item of this list.** A read-only pass (`out/wh/datum-reconcile.md`, §1-§6) re-fetched and re-read the three datum sheets at native resolution and compared their chain against the storey structure `src/whitehouse/layout.js` carries. The chain **as printed on the sheets** is GROUND FLOOR **-12'-8 1/2"**, FIRST FLOOR **0'-0"** (the reference), SECOND FLOOR **+20'-11 1/2"**, THIRD FLOOR **+38'-10"**, PROMENADE **+42'-0"**, TOP OF ROOF **+55'-2 1/2"**, identical to the inch on LOC indices 07, 08 and 10 (printed sheets 31 North, 32 West and 34 East), and **absent** from index 06 (sheet 30) and index 09 (the real South elevation, sheet 33): 30 carries only the -12'-8 1/2" value inside its own dimension string and 33 carries no isolated chain column at all. Both chains are quoted from the same **FIRST FLOOR = 0'-0"**, so this is **not a datum shift** — no offset and no scale converts one into the other, because the **intervals** disagree: first-floor-to-roof **55'-2 1/2"** on the sheets against the **38'-0"** this entry's Source 2 states, and ground-floor-to-first-floor **12'-8 1/2"** against **6'-8"**.
    What the arithmetic settles is that the fault is in this record and not in the sheets: the model's **own** intervals sum to **55'-6"** — the HABS 55'-2 1/2" to 3 1/2 inches — so its intervals are the sheets' numbers and its stated **38'-0" total is the error**, and it carries **no row at all** for the third floor (+38'-10") or the promenade (+42'-0"). The building the chain describes has **three** storeys above the first floor; the model carries **two**, and its +30'-0" and -6'-8" reproduce nothing on the sheets — -6'-8" is -12'-8 1/2" **halved**, and +30'-0" is a real sheet figure put on the wrong floor.
    What the model *does* reproduce is the WHHA published plan and grade set — **168 ft**, **85 ft 6 in**, **50 ft 4 in**, **60 ft** and **9 ft 8 in** — and **no published storey figure**; its -6'-8" and +30'-0" appear on no sheet. The published pair cannot be brought to bear either: the chain's 55'-2 1/2" is above the **FIRST FLOOR** while the published 60'-4" is above the **NORTH GRADE**, and the sheets carry **no spot elevation and no grade symbol**, so which plane the chain's own ground floor is cannot be read off them. This is recorded as a **limit, not resolved**.
    The facade consequence, as arithmetic through this scene's own camera (`v = 0.5 - (y - 9.086) / (2 * 0.54092 * 47.863)`, the file's own transform, validated against its own anchors): the published **50'-4" parapet projects to v 0.3792 against the photographed 0.3800 — 0.2 px** — so the frame's zero **is** the north grade and **the parapet does not move**. What the chain bears on is the storey rows **between** the two photographed anchors, the wall base v 0.6180 and the parapet v 0.3800: the **second floor 0.5382 -> 0.5521** and the **first 0.6363 -> 0.6655**, provisional because whether the chain's GROUND FLOOR is the north grade or the areaway level is **not established** (putting it on the areaway moves the base row 0.6180 -> 0.6655 instead), and the model **needs new rows** at **v 0.4469** (third floor) and **v 0.4282** (promenade). Recorded as residual 1, with the warning that came with it: the pass measuring the window heights must **not** read these row shifts as its own, because its own evidence is the photograph's glass bands — **first floor 3.49 -> 5.63 m, second 8.05 -> 10.93 m** — and those need no chain.

- **The sheet citations corrected against the verification, and one correction the git record does not carry.** The two section-level source citations in "What the model is built from" were corrected against `out/wh/habs-index-verify.md`: the sheet whose window drawing settles the facade is **sheet 82** (index `00040v`), its sheet number having been taken from its index, and the level datums are on **sheets 31, 32 and 34** — the north, west and east elevations, the South elevation being sheet 33 — where the prose had named only the east and the west one, in the wrong order. The same class of citation was corrected with them: the datum chain the apron entry below and residual 7 had attributed to index 12 (`00012v`), which is sheet 37, the north elevation's east half, and carries no datum chain, so both now name the figure's real carrier, the WHHA height pair in `DIMS.southLawnDrop`; and the apron's own elevation pair, cited by index rather than by sheet, now reads as sheets 32 (West) and 34 (East). One inaccuracy in the git history is recorded here rather than repaired: commit `615cb9d`'s message says the wrong projection sign lived in comments in `portico.js` "and one more in `building.js`", but the defect in `building.js` was a stale SCALE (0.01714 for 0.019312), not the sign — a different defect, fixed there for that reason — and the history is not rewritten, so this line is the record.
- **The apron sourced — a step, not a ramp — and it was the last unsourced geometry on this scene.** `src/whitehouse/grounds.js` had carried a 3.1 degree, 40.6 m ramp between the north lawn's south edge and the south lawn's top since the pass that closed a 40.6 x 1040 m trench to the sky; it was invented to close that trench, not measured, and this entry had carried it as residual (f) ever since. The HABS DC-37 sheets were read for it at native resolution with Pillow, because the repo's own CCITT-G4 decoder (`out/wh/scratch/tiff.mjs`) silently returns blank paper for these TIFFs (`tiffdiag.mjs` has the diagnosis). What they say: sheet 32 (West elevation) and sheet 34 (East elevation) each draw the main block's rusticated ground-floor base as ONE HORIZONTAL LINE end to end and the grade change as a STEP — a vertical face with the lower grade at its foot — at the block's south end, with the east sheet the exact mirror of the west; the step's size is the 9 ft 8 in (2.95 m) the WHHA height pair already carries in `DIMS.southLawnDrop`; sheets 2 and 3 (site and landscape plans, 1/50 inch to the foot, the site one after the 1932 USGS contour survey) carry NO contour on the axis between the two fronts and no spot elevation at the ends, so they settle the ARRANGEMENT — two levels and a face, not a ramp — but NOT the face's z, which is the one choice left unsourced here and is flagged as such at `src/whitehouse/grounds.js`. Shipped: the apron is flat at the north grade, the level change is the south lawn's own north face at z -40.6, and the two south magnolias moved z -38.1 -> -48.6, because flat ground would otherwise have left their trunks 3 m in the air. `groundcover` stays green at 0 of 5,915 blank. The scored frame DID move: 11,009 px of 1,080,000 (1.02%) in v 0.508-0.617, mean absolute luma change 8.97, because the apron plane is occluded by the building but the ground behind it is 3 m higher and its SHADOW on the visible ground changed. Scores 0.0949 / 0.4207 -> 0.0950 / 0.4206, each 0.0001 against the repo's 0.0010 noise floor and both inside the tightened thresholds 0.0968 / 0.4157; `measure.mjs` reads detail 0.893, edge 0.861, p5 ratio 1.07, below-16 6.94% and p1 ratio 2.64; the eight calibrated rows read 0.0 px. Landed in `1f35fee`; evidence `out/wh/pass-e-handoff.md`.
- **Pass H — `blackframe` caught the crowns, and the limit was right.** The frame's dark end landed on the photograph's own row in pass B, but that pass's post-handoff tidy-up (`leafSpread 1.75 -> 1.50`, `src/whitehouse/foliage.js`) left a 160x90 tile 98.8% near-black against `blackframe`'s 98% limit, five of its twelve views red. The decisive measurement is the photograph read through the gate's OWN statistic (`out/wh/scratch/blackframe-photo.mjs`): `whitehouse.webp`'s darkest tile is **64.0%** near-black and its next darkest 88.3 / 86.8 / 59.7 / 57.5, so the limit was right and the render's solid 7,120-of-7,200-pixel black rectangle was genuinely too dark; `tools/blackframe.js` was not touched. `leafSpread` is **2.10**, which clears the worst of the twelve rows at 95.2% rather than 1.75's 0.2-point margin, for 1.6e-5 of cell distance and 4.0e-5 of SSIM — under the repo's own 0.0010 noise floor. `SCENE=whitehouse npm test` PASS, twice. Landed in `2f1de21`.
- **Pass F/E — a gate for ground voids, red-proved on the real defect, and the terrain mended.** `tools/groundcover.js` drops a ray from 400 m straight down on a 20 m grid over x +-640 m, z +-900 m and requires 0 of 5,915 cells to reach the sky dome, with a 5,000-cell floor and a sky-name floor so a sweep that measured nothing cannot read as a pass. Red on the real defect before any fix, not on a mutation: **4,166 of 5,915 blank** on scene tree `95fb02b6` — the whole rim of four slabs that all stopped at `TERRAIN_HALF` 520 m and at their own z ends, on a claim ("+-520 m covers the frustum from any camera the clamp allows") measured false because the clamp reaches 634.6 m. Green after **0 of 5,915**; both floors were red-proved too, and the first sky-floor attempt changed a line `b.add(mesh, 'sky')` overwrites and stayed green, which `docs/learning/gate-proofs.md` records. Fixed: `TERRAIN_HALF` 520 -> 680, far ground 216 -> 850 m deep, south lawn to z -940.6. The x and north extensions are byte-identical on the scored frame; the south one moves 2,185 px of 1,080,000, because the south slab's far edge IS where the frame draws its horizon and it had been showing sky below it. Scores 0.09492570 / 0.42069753 -> **0.09490477 / 0.42077211**. Landed in `2f1de21`; evidence `out/wh/pass-f-handoff.md`, `out/wh/pass-h-handoff.md`, `docs/learning/gate-proofs.md`.
- **Pass B — 150 and 175 facet-toned lobes, and the frame's dark end landed on the photograph's.** Each framing crown was 18 and 16 flat-shaded spheres, so a crown read as a cluster of discs with sky in the gaps; the east crown's top half was **0.0%** below luma 16 where the photograph has 63.1%, and 27.6% of its box was sky against 9.0%. It is now 150 and 175 lobes at 0.13-0.20 and 0.12-0.19 of the same envelope, merged to one draw call per crown with a tone per FACET (each lobe `toNonIndexed()`, the facet's own normal picking a place on a dark-biased ramp between the three sampled anchors): the east crown reads 71.3% below 16 in its top half and its mean luma went 35.6 -> 15.3 against the photograph's 14.5. Luma **p5 is 7.5026 against the photograph's 7.5026 — ratio 1.0026**, where it was 14.36 (1.91), which is the line the closing handoff recorded as known and unsolved. Scores 0.0975 / 0.4138 -> **0.0949 / 0.4207**; draw calls 1749 -> 1690 while triangles went 127 k -> 188 k. `nudge` unchanged, and every calibrated row the detector can find is byte-identical between the two renders. Landed in `eb23990`; evidence `out/wh/pass-b-handoff.md`.
- **Pass C/C2 — the lawn's blade speckle, and the hue regression the colour path caused.** The lawn's own sd was 2.77 against the photograph's 29.50 and its mean |gradient| 0.124 against 23.47: a flat grey-green field in a third of the frame. `src/whitehouse/grass.js` (new) is a procedural blade-scale modulation on the lawn's albedo, bound to the `aoMap` slot on a second UV set, because `map` would apply the albedo twice (measured: 28.77 through the albedo slot against 110.03 flat) and because a map on the albedo path is multiplied by the material's colour as well as the vertex colours. That first cut gained detail 0.580 -> 0.912 and edge 0.396 -> 0.852 and **cost the score**, 0.1009 -> 0.1055, and the attribution tool (`out/wh/scratch/whattr.mjs`, the metrics' own 24x22 cells partitioned by region) put **85% of the whole regression on the 120 lawn cells** as a per-channel offset at a constant luma: one fitted scalar (5.87) on top of three unequal tone-curve ratios (0.780 / 1.153 / 0.160) had moved the lawn's hue, losing 34 of its 47 blue levels. The fix is per-channel and derived rather than fitted — `albedoScaleOf(LAWN_BASE) / mean x [1.06653 x 0.91, 1.05874, 1.03092 x 0.60]` with no `albedoOf` and no fitted level left — and the texture could then be free: **0.0975 / 0.4138**, detail held at 0.905 and edge at 0.863. The lawn cells' distance to the photograph went 0.0368 -> 0.0266. Two negative results are worth keeping: the brief's own prescription, a symmetric mean-0.85 map, scored **worse** (0.1101) because it inherited the same unequal-ratio bug, and the map's "0.66345 mean" was a LINEAR mean, not a byte mean (`toSrgb(0.66345) = 0.841`). Landed in `642980b`; evidence `out/wh/pass-c-handoff.md`, `out/wh/pass-c2-handoff.md`.
- **Pass G — the measured roofscape, and the pediment's raking cornices crossing above the apex.** The photograph's silhouette was read column by column (`out/wh/scratch/roofline-tops.mjs`, `roofline-diff.mjs`, `roofline-ray.mjs`) and the roofscape rebuilt to it: a hip roof with its ridge at 18.12 m, two white rooftop blocks flanking the pediment, four chimney stacks with caps and masts, a recessed panel, and a flag on its pole, each with its sampling box in `src/whitehouse/layout.js`. The pediment's top read flat at v 0.3044-0.3122 because `pediment()` seated each rake's centre so its upper-inner corner stood at `yApex + rake / 2` = 18.11 m over a 1.90 m gable: **the two rakes crossed 0.35 m above the apex and their crossed tips, not the apex, were the top edge** — the ray listing named them at (-3.6, 17.5, 6.5) and (-1.2, 17.9, 6.5). Seating them on the apex and measuring the rake's outer end (`RAKE_HALF_WIDTH = 9.0`) closes the spike, and the silhouette now descends 0.3189 -> 0.3044 against the photograph's -0.537 slope. The "the blocks are merging tonally with the pediment" hypothesis is disproved with numbers: the render's block face reads `#76818b` against the photograph's `#818d9a`, and the blocks were OCCLUDED, not merged. Scores 0.1019 / 0.4151 -> **0.1009 / 0.4198**. Landed in `f822aa6`; evidence `out/wh/pass-g-handoff.md`.
- **Pass A — the porch's tones: the shadow hypothesis DISPROVED, and the occlusion term's name-inversion.** The brief's theory was that `RIG.shadowNormalBias` (0.6 m against rakes 0.42 m thick) was bleeding light into the porch's recess. Bisected 0.30 / 0.15 / 0.05 over three renders and **no porch surface moved** (shafts 159 / 159 / 159, tympanum 125 / 125 / 124, recess wall 79 / 79 / 79), so `src/whitehouse/lighting.js` was reverted byte-clean. The decomposition that says why (`out/wh/scratch/wh3shaftlight.txt`, one light pulled at a time) is that the porch is lit ~97% by the two hemisphere lights plus the PMREM and **the sun carries 2-8 luma of a surface near 100**, so a shadow lever cannot move it. What does move it is the `occlusion` term in `src/materials.js`, and it is **NAME-INVERTED**: `albedoOf` divides by `irradiance * occlusion`, so RAISING the value DARKENS the surface. Tympanum 0.44 -> 0.65 lands its mean on the photograph's 98 exactly (0.70 overshoots to 93) and column shafts 0.55 -> 0.85 take the shafts ratio from 1.41 to 1.12 at no cost to any other surface; cell distance and the shafts ratio both favour 0.85, and the 0.0010 of SSIM that favours 0.75 is inside noise. Scores 0.1040 / 0.4136 -> **0.1019 / 0.4151**. Piers-and-lintel was not built: its precondition was shade on the recess wall, the shade does not exist, and the wall is already correct at ratio 1.04. Landed in `d190b95`; evidence `out/wh/pass-a-handoff.md`.
- **The scores file's split brain, consolidated.** Two scores files existed at once, `docs/work/2_white-house-scene/scores.md` and a hyphen-path stray `docs/work/2-white-house-scene/scores.md`, and the stray is the one that had been deleted and restored twice in the preceding commits (`76a56fc`, `ec00564`). The canonical path is the one `src/scenes.js` names in the whitehouse entry's `thresholds`, the underscore form, so the stray was removed and the surviving file carries the thresholds the suite asserts; `4d7cdbf` is that consolidation, and it also records why this file holds exactly one fenced json block (tools/test.js takes the FIRST block it finds, so a second one above the thresholds is parsed as the thresholds and fails naming a word from the prose).
- **The scene is built, scored and green.** `SCENE=whitehouse npm test`: `shot`, `compare`, `nudge` and `blackframe` pass, the five scene-1 gates are skipped with their reasons printed, and the scores are asserted against `docs/work/2_white-house-scene/scores.md` at **0.1450 / 0.1269** (thresholds 0.1477 / 0.1212). `npm test` for scene 1 is still PASS, ten gates, contract frame `e95a53185ee3` on a 27-file tree at 0.0596 / 0.5713.
- Three visual defects were found by looking at orbit frames the scored view cannot see, and all three are fixed: the south tree line hid the building from due south, a near-black slab lay beside the west end (it was the void past the lawn's own edge, not a mesh), and the north lawn's end hedges read as separate balls rather than one run. The last was the coordinator's own edit, made after the handoff.
- An independent read-only review of the tooling half found two defects, both fixed in `0319f17` and both reproduced first: `nudge` and `blackframe` were declared scene-neutral while opening the bare URL, so they reported scene 1's verdict under another scene's name; and `sourceTree()` hashed every scene's files, so a second scene's edit deleted scene 1's render mid-run. `tools/test.js` now requires every scene-neutral gate to print which scene it measured.
- The camera was calibrated against the photograph's own measured edges rather than its EXIF, which disagrees with itself by 19 degrees. Four landmarks land within 2 px; the portico's pediment apex carries an 82 px residual, recorded below.
- Three read-only research handoffs landed: the shared-module contract (`out/wh/contract.md`), the photograph and dimensions dossier (`out/wh/research-photo.md`), and a second sourced dimension dossier (`out/wh/research-dims.md`). Between them they corrected four things the brief had wrong (flat roof, no South Portico pediment, tetrastyle north against hexastyle south, 11 bays on both fronts) and located the HABS measured drawings.
- Tool integration landed and proved: `src/scenes.js` carries one entry per scene (`photo`, `photoSize`, `shot`, `out`, `thresholds`, `sourcePaths`), `tools/lib/scene.js` resolves the active one from `SCENE`, `shot` and `compare` are scene-scoped, and `test.js` runs each scene's own gates and thresholds. Scene 1 unmoved: `out/render.png` still `e95a53185ee3…`, scores still 0.0596 / 0.5713, `import-inert` green over all tools.
- (open) Entry created. Research into the reference photograph and the shared-module contract dispatched as two read-only subagents.

## Known residuals, and what a next pass should do first

Recorded rather than left to be discovered, in the order a next pass should take them. Every one of these is
measured; each carries the number a next pass starts from.

1. **The facade's storey structure: the model carries two storeys above the first floor where the datum chain describes three, and the discrepancy is confined to the rows between the wall base and the parapet.** The HABS chain the model's levels come from prints six values from a FIRST FLOOR = 0'-0" reference — GROUND FLOOR -12'-8 1/2", SECOND FLOOR +20'-11 1/2", THIRD FLOOR +38'-10", PROMENADE +42'-0", TOP OF ROOF +55'-2 1/2" — where this entry's Source 2 and `src/whitehouse/layout.js` carry ground floor -6'-8", first floor +10'-0", second floor +30'-0", cornice +64'-6", roof ~+65'-6" and a first-floor-to-roof total of 38'-0". Both chains are quoted from the same first-floor zero, so it is **not** a datum shift: the **intervals** disagree, first-floor-to-roof 55'-2 1/2" on the sheets against this entry's 38'-0", and ground-floor-to-first-floor 12'-8 1/2" against 6'-8". The model's own intervals sum to 55'-6" — the HABS 55'-2 1/2" to 3 1/2 inches — so its intervals are the sheets' numbers and its stated **38'-0" total is the error**; and it has **no row at all** for the third floor or the promenade, where the building the chain describes has three storeys above the first floor and the model carries two. The two rows that do sit between the wall base v 0.6180 and the parapet v 0.3800 move: the second floor v 0.5382 -> **0.5521** and the first floor v 0.6363 -> **0.6655**, and the model needs **new rows at v 0.4469** (third floor) and **v 0.4282** (promenade). Those rows are provisional, because whether the chain's GROUND FLOOR is the north grade or the areaway level is not established — the sheets carry no spot elevation and no grade symbol, which is also why the chain's 55'-2 1/2" above the FIRST FLOOR and the published 60'-4" above the NORTH GRADE cannot be reconciled against each other from these sheets, stated as a limit and not resolved. What the model does reproduce is the published WHHA plan and grade set — 168 ft, 85 ft 6 in, 50 ft 4 in, 60 ft and 9 ft 8 in — and no published storey figure; its -6'-8" and +30'-0" appear on no sheet. The 50'-4" parapet projects through this scene's own camera to **v 0.3792 against the photographed 0.3800, 0.2 px**, so the frame's zero IS the north grade and the **parapet does not move**. A pass that owns `src/` should re-read **sheets 31, 32 and 34 at native resolution** — crops already in `out/wh/habs-verify/` and `out/wh/datum-reconcile/` — and **settle the chain's zero before moving any row**. Evidence: `out/wh/datum-reconcile.md` §1-§4. And the same pass should repair `src/whitehouse/layout.js`'s own citation for the chain, "HABS measured South elevation (sheet 34, read in habs-findings.md)", which is **wrong twice over**: sheet 34 is the **East** elevation, and the South is **sheet 33 at index `00009v`** — so the chain the model carries was not read off the sheet it names, whichever the name is taken to mean. The warning that came with it: the pass measuring the window heights must **not** read these row shifts as its own, because its own evidence is the photograph's glass bands (first floor 3.49 -> 5.63 m, second 8.05 -> 10.93 m), which need no chain.
2. **The pediment's eave cornice is drawn at the portico's full width where the photograph's gable base is narrower, and its entablature occludes the photograph's own dark roof band at u 0.3475-0.3625 and u 0.6325-0.6575 by 24-32 px.** At u 0.3475 the photograph's band starts at v 0.3711 with tone `#050b0f` and the render's first non-sky hit there is `north portico pediment eave cornice` at (-8.9, 15.3, 6.9) at v 0.3356 reading `#7f95d4` — **-32 px**; at u 0.3500-0.3600 it is **-25 px**, and the east side mirrors it at **-24 px** (u 0.6325) and **-31 px** (u 0.6575). The ray listing names the order of cover: at (u 0.35, v 0.360) the eave cornice, then `north portico cornice`, then `north portico frieze` at (u 0.35, v 0.372), with `roof north slope` behind all three at d 64.7. The eave cornice's top is 16.00 m at the porch front z +6.5 and projects to image u 0.3258-0.6742 while the band's row inverts to about 14.6 m there, so the portico's entablature covers v 0.343-0.380, which is the row the photograph's band occupies. Where the eave is out of the way, at u 0.6850-0.7200, the render's band reads `#0c0e18` against the photograph's `#09050b`. `RAKE_HALF_WIDTH = 9.0` fixed the raking cornice's own extent; the eave still runs to the portico's corners. Either the eave's projected top is 1.3 m too high at its outer ends or the pediment's eave should not be drawn to the portico's full width: this is a portico width/depth decision, and both arms move the photograph's own eave row, so it needs its own measured pass rather than a nudge. Evidence: `out/wh/pass-g-handoff.md` §3b and §6.
3. **The west crown is 10 points past the photograph on below-16 and 13 on sky at once** — 69.7% below luma 16 in its own box against the photograph's 56.3%, and 35.0% sky against 22.3%. It is too dark AND too open, which is not a contradiction: its lobes are large enough to leave gaps and dark enough that what fills them is black. Every lobe-field variant tried put the two fractions on opposite sides of the photograph's, and the sweep that would settle it is west-only (gain and clump amplitude). Evidence: `out/wh/pass-b-handoff.md` §2 and §6.
4. **The lawn's speckle matches the photograph's high-pass sd but has no mowing-band structure and no large soft blotches, and it reads coarser than the photograph at 3x.** Measured over u 0.20-0.80 v 0.90-0.98: render sd 28.21 against the photograph's 29.50, mean |gradient| 21.06 against 23.47, and the radius profile 14.39 / 21.58 / 24.64 / 27.09 / 27.88 against 21.55 / 25.39 / 26.66 / 27.97 / 28.86 — close at every radius, and still an even aperiodic mottle where the photograph has structure. Evidence: `out/wh/pass-c2-handoff.md` §5.
5. **The frame's darkest 1% is lighter than the photograph's while its 5th percentile matches.** `measure.mjs` on the shipped frame reads luma p5 7.50 photograph / 8.05 render (ratio **1.07**) and luma p1 1.00 / 2.64 (ratio **2.64**). Pass B landed p5 exactly and p1 moved 1.07 -> 2.86 -> 2.64 with it, so the shadow floor is right in mass and too light in its very darkest pixels. Evidence: `node out/critic/measure.mjs whitehouse.webp out/wh/render.png` on the shipped frame.
6. **The two framing trees still stand 18 and 16 m SOUTH of the north wall** — the deliberate incoherence. They were tried at z +18 / +16 by an angular-preserving transform about each tree's own ground point (ratios 0.4533 and 0.4989) and reverted, with the reason measured: `crownCluster` places its lobes at absolute fractions of the envelope and sizes them at 0.20-0.33 of it, so the mass a lobe field makes is a property of the radius in metres and not of the angle it subtends — the west crown's arrival row went from +5 px to **+144 px** at u 0.02 and the frame from 0.1031 / 0.3918 to 0.1153 / 0.3687. A future pass would have to re-solve `spread`, `size`, `n` and the polar lobe's own 0.30 per tree for the new radius. Evidence: `out/wh/closing-handoff.md` §6.
7. **The apron between the north and south lawns: the arrangement is SOURCED; only the face's z is not, and the site and landscape plans carry no contour on the axis.** HABS DC-37 sheets 32 (West elevation) and 34 (East elevation) each draw the main block's rusticated ground-floor base as one horizontal line end to end and the grade change as a **step** — a vertical face with the lower grade at its foot — at the block's south end, the east sheet the exact mirror of the west, and the step's size is the 9 ft 8 in (2.95 m) the figure `DIMS.southLawnDrop` already carries. Sheets 2 and 3 (site and landscape plans, 1/50 inch to the foot, the site one made after the 1932 USGS contour survey) carry **no contour on the axis between the two fronts and no spot elevation at the ends**, so they settle two levels and a face rather than a 40.6 m ramp but cannot place the face along z. What remains open is therefore only that z: it stands in the code at the south lawn's own north edge, z -40.6, which is the bow of the south portico and the only edge derivable in this scene, and a future pass with the Olmsted 1935 topographical survey (CLR figure 6-3) or the Latrobe/Bulfinch drawings could move it north to the terrace's own brow. `src/whitehouse/grounds.js` says so at the point it decides. Evidence: `out/wh/pass-e-handoff.md` §1-§2.
8. **`tools/groundcover.js` cannot see a hole outside x +-640 m / z +-900 m, narrower than one 20 m cell, or hidden under another mesh**, and because `clampCamera` never reads x or z a panned camera can be put over ground no finite box contains. Its own header says all four. The box is 640 = the clamp's 260 m of orbit distance plus 0.7213 x 520 of the frame's half-width at the scene's old ground edge, and 900 = `fogFor`'s far distance, past which ground and sky cannot be told apart. Evidence: `tools/groundcover.js`'s header and `out/wh/pass-f-handoff.md` §2.

Carried from the earlier record, still true: the camera's eye height is 9.1 m above the north lawn because the photograph's four landmarks fix `tanH * d` and `tanV * d` rather than `d` (changing it moves the wall base off the row the photograph measures); the south front is bare below the balustrade, with no window pediments, no belt course and no ground-floor rustication; the fountain is three cylinders and two spheres and the fence's pickets are a uniform box array; and `compare`'s overlay draws scene 1's landmark boxes for every scene, so `out/wh/overlay.png` labels the White House "cherry" (annotation only, it moves no score; the correct one is `out/wh/overlay-wh.png`). Of those, the fountain and the pickets are the two a next pass can act on.

