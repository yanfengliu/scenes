# Realism and content: a standing improvement loop on the Japan street scene

Status: active
Owner: Manager session (integration owner)
Created: 2026-09-10
Updated: 2026-09-16

## Problem and outcome

The user's direction on 2026-09-10: "Keep iterating to make the scene look better. More realistic, and more content." The scene at e725914 scores 0.0812 cell distance and 0.4401 SSIM against the photo and reads, beside it, as paler and flatter: the photo's darks (timber, eave undersides, the bend, the ground floor under the right eave) are near black where the render is mid grey, and the photo's lit stone and plaster are lighter than the render's. From other angles, what the photo hides is still block-out: the corner house at the bend, the far houses, the roof undersides and the right roof mass.

Outcome: a scene that scores closer to the photo on the compare gate at every iteration, never worse, and that holds up when orbited, with the block-out retired and the street carrying the life the photo shows. This is a loop, not a fixed list: each iteration's target comes from looking at the rendered scene and the compare sheet, not from reading code for something to improve.

## Scope

Included: materials and colors resampled from the photo, geometry the photo pins down, content the photo shows or implies, the sky and atmosphere, the cherry's depth, close-up material detail for orbit views, and one instrument the loop needs, `tools/views.js`, which renders a fixed orbit sweep to `out/views/` so every iteration is inspected from the same angles.

Excluded: the camera contract in `src/layout.js`, any loosening of the thresholds in `docs/PLAN-scores.md`, downloaded assets, a build step, and any file the concurrent CI-gates worker owns (`tools/test.js`, `tools/lib/browser.js`, `tools/blackframe.js`, `tools/record.js`, `tools/paintcheck.js`, `tools/shimmer.js`, `.github/workflows/ci.yml`) until that work is integrated. Workers do not edit `AGENTS.md` or `docs/devlog/summary.md`; the integration owner carries those at integration to avoid merge conflicts.

## Approach

One worker per iteration, in its own worktree from main, with a brief that names the measured cells and the visual gaps it must close, the files it may touch, and the numbers it must report. The integration owner inspects the handoff, runs the full suite in the primary checkout, looks at the photo view and the orbit sweep at native resolution, integrates, pushes, and watches CI to a conclusion. The next iteration's target is chosen after that look, from the compare heat map and the sweep.

The measured gap at the start, the twenty worst of 528 cells, groups into four causes. Contrast: the photo's near-black timber and undersides render as mid brown or grey (u 0.15 v 0.34: photo #38241c, render #766554; u 0.69 v 0.57: photo #22201e, render #5e5a5a; u 0.85 v 0.52: photo #4e453c, render #7c797b; u 0.98 v 0.25: photo #56432f, render #87786e), and the photo's lit stone and plaster render too dark (u 0.35 v 0.75: photo #a7abb2, render #696a6d; u 0.06 v 0.57: photo #c5b8aa, render #866f5d; u 0.77 v 0.89: photo #798288, render #44484c). Placement: the nearest left house reaches into cells the photo shows as sky and cloud (u 0.15 v 0.11: photo #cfbfb5, render #716c71; u 0.06 v 0.07: photo #949ca3, render #5e6067), and the annex canopy renders grey tile where the photo has dark wood (u 0.23 v 0.48: photo #66543e, render #8a8c90). The bend: the far street darkens into the bend in the photo and stays light in the render (u 0.44 v 0.75: photo #323840, render #6d6d73; u 0.56 v 0.75: photo #454446, render #757a85). Color: the shrub is greener (u 0.77 v 0.66: photo #7c958b, render #5c544e), the noren carries the sky's blue (u 0.98 v 0.43: photo #91a9c9, render #6f737f), and the sky at the top right eave is brighter (u 0.85 v 0.02: photo #ecedec, render #b9b4b2). The bottom half of the frame carries more error than the top (12.1 and 12.3 against 9.7 and 8.8 per quarter).

A global grade was tried in phase 4 and made the scores worse, so the contrast is fixed per material, by resampling from cleaner photo regions and by checking what the rig's ambient share does to a dark albedo, never by a frame-wide curve.

## Acceptance criteria

Per iteration, all of them, before the integration owner pushes:

- [ ] Cell distance and SSIM not worse than the thresholds; thresholds tightened to the achieved values minus the recorded margin.
- [ ] `npm test` green in the primary checkout: import-inert, shot, compare, placement, namerules, clearance, animation, nudge, blackframe, record.
- [ ] The photo view and the orbit sweep from `tools/views.js` inspected at native resolution by the integration owner, looking for what is wrong rather than for what changed.
- [ ] One independent critic on the worker's diff and claim, findings resolved, recorded in the worker's detailed devlog section.
- [ ] Committed to main by pathspec, pushed, and the remote gate watched to a conclusion.

## Implementation steps

- [x] Iteration 1, contrast and placement (merged cb9a17c): close the measured cells above and build `tools/views.js`. Target: cell distance at or below 0.077, SSIM at or above 0.45, or the measured ceiling reported with what stops it.
- [x] Iteration 2, content (merged 28bd01d, gated 3c646d2): the machiya row and the bare slab; the rest of the block-out carries to a later iteration. Was: retire the block-out the orbit sweep shows. The corner house at the bend, the far houses as machiya with tiles and lattices, roof undersides with rafters, the right roof mass as tiles, the dormer body, the paved bands.
- [x] Iteration 3, re-targeted from the heat map on 2026-09-15 (merged 2026-09-16): the right half and the bottom third, where the error moved after iteration 2, plus two iteration-1 survivors (the annex canopy still tile where the photo has dark wood; the shrub still brown) and shopfronts on the far row. Sky and atmosphere move to iteration 4; the fixed list below shifts by one.
- [x] Iteration 4, re-targeted 2026-09-16 (merged a78ba8f): content every orbit pose shows wrong — the hill and mountains, ground fog bands, the left row's backs, the right roof mass, the bend — plus the right-machiya cells that survived three colour iterations, treated as shadow and placement. The sky band now scores better than the frame mean (0.058 against 0.066) and the canopy box best of all (0.034), so sky and the cherry's depth move down. Was: the cherry's depth: dark limbs visible through the canopy, a shadowed magenta interior against backlit edges, strand structure, card variety.
- [x] Iteration 5, re-targeted 2026-09-17 (merged 09eb339): the bottom third by measurement, the corner, the machiya trio, the glare overshoot, cost-neutral. Was: street life: stone variation and wetness, the drain, moss, two or three more figures, shop goods under the noren, signs, pots, a bicycle, only where the photo view does not regress.
- [x] Shot-rung lane, 2026-09-17 (merged 60c3dda): the rung was not the mechanism, and forcing all five of them proved it. A second nondeterminism was found and fixed (the black-frame watchdog ratcheting on an unreadable drawing buffer, 1 run in 25), the contract frame now records and refuses the configuration that drew it, and the original frame's class is detected by compare's same-tree light check rather than solved.
- [ ] Iteration 6, re-targeted 2026-09-17: the sweep's largest wrongs as geometry — the hill's near face, the right machiya's roof mass, the far houses' block-out — plus the bottom-left quarter by measurement. Was: close-up realism: materials at orbit distance, tile and board edges, wood grain, foliage cards that hold up close.
- [ ] Fast arm, opened 2026-09-17 (in flight): the loop's inner step is a CPU rasterizer on a machine with a GPU. The 2026-09-15 move fixed the gates and not the arm: `shot` is 57 s, correctly SwiftShader, and it is still the only render-and-score path in the repo, while `animation` already scores seven frames on the GPU in 13 s with the same metrics code. Target an arm under 10 s, with `shot` and `compare` untouched.
- [ ] Environment-map proof, queued behind iteration 6: `src/lighting.js` builds a PMREM of the sky dome and never looks at it, and a run where it carried no light is what the 2026-09-17 flake's signature says the frame was. Prove it the way `src/post.js` proves the post chain — render it, look at it, fail loudly — which is the shot-rung lane's own recommendation and was outside its files. Serialized rather than run beside an iteration: both lanes are CPU-bound on the same SwiftShader shots, and the flake being hunted is load-correlated.
- [ ] Then the loop again, from the heat map and the sweep.

## Lanes

One expensive gate runs on this machine at a time (a full `npm test`, or `animation`, `nudge`, `views` or `record` alone); a lane asks the integration owner for the slot before starting one and reports when it ends. Two suites side by side crashed the browser under `nudge` on 2026-09-15 and inflated `animation` fourfold. Lanes never share a file: scene lanes own `src/` and `docs/PLAN-scores.md`; tool lanes own `tools/`; the integration owner owns `AGENTS.md`, `docs/devlog/summary.md` and this file.

| lane | owner | outcome | files | waiting on |
| --- | --- | --- | --- | --- |
| iteration 3 | done: merged 2026-09-16, 0.0660 / 0.5286 | right-side stone and kawara, two iteration-1 survivors, shopfronts on the far row | src/, PLAN-scores.md, its devlog | nothing |
| gpu-gates | done: merged 0b81d68, suite green on main at 2 min 46 s | every gate whose verdict allows it renders on the GPU; shot stays on SwiftShader and refuses a GPU | tools/, AGENTS.md Gates, gate-proofs | nothing |
| iteration 4 | done: merged a78ba8f, 0.0640 / 0.5403 | content from the sweep: the hill and mountains as geometry, ground fog bands, the left row's open backs, the right roof mass, the bend; the persistent right-machiya cells treated as shadow and placement | src/, PLAN-scores.md, its devlog | nothing |
| tools-followups | done: merged 6f12960 | clearance classifies the new walkway slabs and spill leaves as buildings; the sweep lost its digest binding to the scored frame when views moved to the GPU | tools/, AGENTS.md Gates, gate-proofs | nothing |
| tools-bend-regex | done: merged aefc71f | narrow clearance's bend exemption to what the scene now produces; a gate that lists every name-matching rule's population and fails on an unstated change, retiring the open lesson | tools/, AGENTS.md Gates, gate-proofs, lessons | nothing |
| iteration 5 | done: merged 09eb339, 0.0596 / 0.5713 | the bottom third of the frame (stone, steps, walls, the fence's base and cap), the bottom-right corner as an occluder or a cast shadow rather than a colour, the right-machiya trio as placement and shading, one glare overshoot from iteration 4; cost-neutral on draw calls | src/, PLAN-scores.md, its devlog | nothing |
| shot-rung | done: merged 60c3dda, contract reproduced | the contract frame is not deterministic run to run (0e56fc677faf against e95a53185ee3 from tree 387c0513bdeda0a5): prove which rung drew the bad frame and why it was chosen, fix the cause, make shot print and record the post state and fail on any rung but the top, make compare refuse a sidecar without it, red-prove each, register the defect | src/post.js, tools/shot.js, tools/compare.js, tools/lib/browser.js, AGENTS.md Gates, gate-proofs, defect register, its devlog | nothing |
| fast-arm | worker fast-arm, worktree from f5819c1 | an iteration lane's A/B arm costs 57 s because `shot` is the only render-and-score path and it is SwiftShader by contract; give a lane a GPU path for questions that are not the contract, with a calibrated margin under which an arm must go back to `shot` | a new tool, package.json, AGENTS.md diagnostics, its devlog | nothing; GPU renders only, SwiftShader calibration batched |
| env-proof | queued behind iteration 6 | prove the sky dome's environment map carries light instead of assuming it, the way the post chain is proved | src/lighting.js, a gate, AGENTS.md Invariants, gate-proofs, its devlog | iteration 6 to land |
| integration | this session | merge, suite in the primary checkout, push, CI to a conclusion | the primary checkout | each lane's handoff |

## Outcome

Iteration 1, merged as cb9a17c. Cell distance 0.0812 to 0.0768 and SSIM 0.4401 to 0.4559, both targets met; thresholds tightened to 0.0783 and 0.4509. The twenty worst cells fall 26% in summed distance. `tools/views.js` now renders seven poses to `out/views/`.

Two findings shape iteration 2. The brief's reading of cells (0.44,0.75) and (0.56,0.75) was wrong: they are landing slabs, and at z -21 the paving's right edge sits at x 2.5 where the photo's street edge is at x -2.0, so about 4.5 m of paving stands where the photo has the machiya row. Four attempts on the paving all lost, because the fix is the row itself. And the views found the top platform's face standing as a bare grey slab across the head of the stairs, invisible from the photo view and glaring from the landing.

`tileRight` is a measured dead end for colour work: it is the pan tiles' S-section, so lifting its hex moves nothing. The cross-machine SSIM noise floor is 0.0010, measured on identical code.

Iteration 2, merged as 28bd01d and gated in 3c646d2. Cell distance 0.0768 to 0.0749 and SSIM 0.4559 to 0.4733; thresholds tightened to 0.0763 and 0.4703. The machiya row lining the far street was measured from the photo, the paving-to-building boundary sampled at six heights with each crossing dropped onto the street's height field, which is why four colour attempts in iteration 1 had lost: 4.5 m of bare paving stood where the photo has buildings. The bare slab at the head of the stairs was a mortar face coplanar with the ashlar. Two defects the scored frame cannot see were fixed at a cost of 0.0020 SSIM and kept: blossoms hung to 1.71 m over a street where a standing eye is 1.70 m, because the photo's blossom mask is a 2D silhouette and cannot bound depth; and the row crossed the road by 0.46 m at its last bay. Both are gated by `npm run clearance`, eight assertions in 34 s, every one proved red with measured boundaries, and the gate reproduced its numbers on CI digit for digit. The SSIM margin is 0.0030 against a measured cross-machine noise floor of 0.0010; `HEADROOM = 2.2` is the dial if that proves thin.

The loop's own cost was the next target, because it is paid on every iteration: the suite went from 35 minutes to 12 locally, merged 2026-09-15 as f2719ac, with the scored render byte-identical, by taking image work off the scene page and merging round-trips. CI is estimated at about 32 minutes from about 70, to be read from the next run rather than believed.

Still wrong in the sweep, for iteration 3: the row is a featureless plank wall with no shopfronts; the far houses and mountain cards are flat boxes and strips with hard edges; the hill is a dark speckled slab with a hard silhouette; the right roof mass is one plane and its back walls bare; the ground is one flat plane with fog banding; stone textures repeat about every metre; the person is a billboard; the strands read as hard wires.

Iteration 3, merged 2026-09-16. Cell distance 0.0749 to 0.0660 and SSIM 0.4733 to 0.5286; thresholds tightened to 0.0675 and 0.5236, the SSIM margin back to a full 0.005. The brief's twenty worst cells fell 32.8% in summed distance, both iteration-1 survivors closed, the far row has shopfronts, and the largest single win was off the list: the right walkway, a flat plane whose mean was already right and whose texture carried the frame's worst summed error. The critic's measurement of the rig (deepest cast shadow 14%, a painted shadow at 81%) is the finding to remember: a surface can score right and still read as paint, and the sweep is where that shows.

Open for the tools lane, from iteration 3: `clearance`'s TERRACE list classifies the new walkway slabs and the fence's spill leaves as buildings (harmless at their x today; the terrace headroom rays land 5 mm under the walking surface); and with `views` on the GPU and `shot` on SwiftShader, the sweep is bound to the scored frame only by source hashes, a weaker claim than the byte identity it had.

Iteration 4, merged a78ba8f. Cell distance 0.0660 to 0.0640 and SSIM 0.5286 to 0.5403; thresholds tightened to 0.0655 and 0.5353. The sky, the hill's skyline, the ranges, the valley floor, the left row's backs and the bend's paving are real geometry now; the right machiya's roof is still one plane and the far houses are block-out. Perf 5.00 ms median and 372 draw calls of 400, 31 of them spent here, so the next content unit must be cost-neutral or pay first. The render on main after the merge is sha256 bfb72a92a72d; the devlog's recorded digest predates the critic's second round, whose shadow fixes moved pixels without moving the scores. Two facts corrected in the record: the sky was the frame's largest single error mass (its 69 cells at 4.208 summed distance), which a mean-per-cell reading had hidden; and the cell at (0.146, 0.386), on the miss list since iteration 1 as house 1's timber, was a hole showing a mountain card a kilometre away.

Iteration 5, merged 09eb339. Cell distance 0.0640 to 0.0596 and SSIM 0.5403 to 0.5713; thresholds tightened to 0.0611 and 0.5663. The frame's two worst cells for three iterations were geometry: the right terrace's near end is a step at one z, and they go 0.2673 to 0.0183 and 0.2484 to 0.0397. The wall-top strip and the fence's wall were each two populations one sample box had averaged; the top eave's edge sits on the photo's line with the ridge held; the hill's glare narrows to 0.055 of frame. Draw calls 372 to 372, so cost-neutral, and the frame time is not quoted because three runs of one tree spread wider than any difference between trees. The render on main after the merge is sha256 e95a53185ee3, tree 387c0513bdeda0a5, the same tree the worktree's sweep 33aa7bfd4276 was drawn from. The first suite on main went red at the threshold check: its shot drew a different frame from that same tree (sha256 0e56fc677faf, 0.0632 / 0.5718, every lit surface darker and the sky untouched), a second shot reproduced the worktree's frame to the digit, and the second full suite passed all ten gates with the shot at e95a53185ee3 (0.0596 / 0.5713). The scored frame is therefore not deterministic run to run on this machine, and neither shot nor compare records which rung of the post chain's ladder drew it; that is opened as its own lane below, ahead of iteration 6, because every threshold the loop records comes from a shot. Two bounds recorded: the name-rules gate pins mesh counts and not instance counts, so the 17 instances the step and coping added to right walkway slabs (84 to 101) are outside what it can see; and the devlog quotes two splits of the same net gain (440 cells better by 3.182 and 88 worse by 0.837 in the cells section; 3.12 and 0.776 in the critic's), which agree on the net of 2.345 and differ in their gross terms. Not reached, carried to iteration 6: the right machiya's roof mass is one plane and the hill's near face is a speckled cliff from the orbit poses.

Shot-rung lane, merged 60c3dda. Opened because the first suite on main after iteration 5 failed its threshold on a frame the same tree had scored 0.05958 in its worktree. **The hypothesis in the brief was wrong and the measurement is what says so**: forcing each of the five rungs the ladder offers at 1200x1100 gives five different files, each within one luma level of the contract, where the bad frame sits seven levels away. That frame is the scene with no environment-map contribution, at correlation 0.985 and 416 of 528 cells exact, and its mechanism is still unproven after 56 fresh shots failed to reproduce it. A second, independent nondeterminism was found on the way and fixed: the black-frame watchdog read nine pixels off the default framebuffer and ratcheted permanently on one black reading, which that buffer can return through no fault of the scene, and 1 of 25 fresh shots drew a lesser rung's frame because of it. A black reading is now a trigger that asks the chain's own verification before it steps down, and a healthy frame still makes exactly one read, so rung-0 pixels do not move: the contract reproduces at sha256 e95a53185ee3 on tree dceabc6abdf61e7c, scoring 0.05957921461787957 / 0.5713434848735502. `npm test` on main after the merge: PASS, ten gates, exit 0 (`out/integration-shotrung.log`), with the sidecar recording the designed rung, a 1440x1320 target at 4 samples, 0 ratchets and 0 false alarms. What is gated: `shot` records the post state in the sidecar and refuses four things a contract frame must never be, with `designed` and not `rung === 0` as the predicate because the memory budget can make index 0 a lesser chain; `compare` refuses a sidecar without that state, a lesser rung, and a same-tree disagreement about how much light is in the scene; every gate echoes the page's post-chain lines, which `collectErrors` could not see at all because it only ever watched `console.error`. Fourteen red proofs. Two bounds worth carrying: the same-tree light check moves its own anchor to the latest run, so one flake produces two reds and the second lands on a correct render, with the third run green; and an always-black drawing-buffer read is indistinguishable from a genuinely black canvas, so that case still walks the ladder down and is caught by `shot` refusing the frame rather than by the chain.
